     1→     1→# CipherChat — Complete Change Record
     2→     2→
     3→     3→A detailed, chronological record of every change and addition made to the
     4→     4→project, cross-referenced against the internal task IDs used in
     5→     5→`worklog.md`. Final state at time of writing: **124/124 property tests
     6→     6→green, lint clean, tsc clean, all services healthy.**
     7→     7→
     8→     8→**What the product is:** an end-to-end-encrypted, ephemeral group chat.
     9→     9→All cryptography runs in the browser; the server is a blind relay that
    10→    10→stores no messages; rooms die on a clock (or by fire); the only identity
    11→    11→is a per-room deterministic alias in a muted ink colour. Single
    12→    12→user-visible route (`/`), hash-routed client-side. Aesthetic: warm
    13→    13→paper, ink, and forest — deliberately *not* cyber/hacker.
    14→    14→
    15→    15→---
    16→    16→
    17→    17→## 1. Foundation (Tasks 0–8)
    18→    18→
    19→    19→### 1.1 Architecture decisions (Task 0)
    20→    20→
    21→    21→- **One route, hash router**: `/` with `#/`, `#/new`, `#/join/:id`,
    22→    22→  `#/rooms`, `#/r/:id`. Invite links use `/?join=<roomId>`.
    23→    23→- **Crypto in the browser**: room key derived from the room password;
    24→    24→  AES-GCM message encryption; per-device signing keys; forged messages
    25→    25→  (bad signature) are quietly rejected.
    26→    26→- **Server is a blind relay**: REST for room lifecycle + member
    27→    27→  registry (keyed by pubkey), socket.io mini-service `:3003` for
    28→    28→  in-memory presence and message relay. **No message persistence —
    29→    29→  new joiners see no history, by design.**
    30→    30→- **Refresh = keys wiped** (memory only) → rooms lock; the room list
    31→    31→  survives in localStorage; locked rooms unlock by re-entering the
    32→    32→  password.
    33→    33→- **Identity**: deterministic alias (adjective + animal, e.g. "Quiet
    34→    34→  Heron") + one of 8 curated muted ink colours, derived from the
    35→    35→  fingerprint of the member's signing pubkey. No red/terracotta hues
    36→    36→  (terracotta is reserved for impermanence/destruction).
    37→    37→
    38→    38→### 1.2 Design system (Task 1)
    39→    39→
    40→    40→- `globals.css`: full token sheet for two themes — **Daylight**
    41→    41→  (paper `#F4F1EB`, side `#EBE7DF`, charcoal `#2C2A28`, forest
    42→    42→  `#3A4F41`, terracotta `#C85A40` for destructive/impermanent only,
    43→    43→  ember `#E8A87C` for motion only) and **Nightfall** (dark, warm,
    44→    44→  low-contrast).
    45→    45→- Typography: **Lora** (the human voice — messages, names, headings),
    46→    46→  **Inter** (the machine voice — chrome, labels, meta), **IBM Plex
    47→    47→  Mono** (fingerprints, codes, timers).
    48→    48→- Grain overlay, custom keyframes (breathe/settle/rise/seal/burn),
    49→    49→  mandatory `prefers-reduced-motion` overrides (150 ms single fades).
    50→    50→- Custom Tailwind tokens: `hairline`, `paper`, `side`, `wash`,
    51→    51→  `charcoal`, `mute`, `forest`, `terracotta`, `ember`.
    52→    52→
    53→    53→### 1.3 Backend (Task 2)
    54→    54→
    55→    55→- **Prisma schema** (`prisma/schema.prisma`): `Room`
    56→    56→  (id / verifier / creatorToken / epoch / burned / expiresAt added
    57→    57→  later) + `Member` (pubkey / alias / colorIdx / active / lastSeenAt,
    58→    58→  unique `[roomId, pubkey]`), SQLite at `db/custom.db`.
    59→    59→- **API routes** (`src/app/api/`): `POST /api/rooms` (create),
    60→    60→  `GET /api/rooms/:id`, `PUT /api/rooms/:id/verifier` (set-once),
    61→    61→  `POST/GET /api/rooms/:id/members` (registry, cap 12, upsert by
    62→    62→  pubkey), `POST /api/rooms/:id/leave` (epoch bump), `POST
    63→    63→  /api/rooms/:id/burn` (creatorToken-gated) — later joined by
    64→    64→  `PATCH /api/rooms/:id` (creator TTL adjust) and `POST
    65→    65→  /api/rooms/:id/evict` (silent-departure eviction).
    66→    66→- **Relay mini-service** (`mini-services/chat-service/`, port 3003,
    67→    67→  socket.io, in-memory): room:join, message:send, message:spent,
    68→    68→  member:leave / member:presence / member:typing / member:expired,
    69→    69→  room:burn / room:state. Blind: no DB, no message storage, 16-room
    70→    70→  cap per socket, frame-size cap → hard disconnect, token-bucket rate
    71→    71→  limit per socket (20 frames/s sustained, burst 64).
    72→    72→
    73→    73→### 1.4 Client libraries (Task 3)
    74→    74→
    75→    75→- `lib/crypto.ts` — WebCrypto wrappers: AES-GCM, ECDSA P-256
    76→    76→  canonical signing, verifier blobs.
    77→    77→- `lib/identity.ts` — wordlist aliases, inks, fingerprint formatting,
    78→    78→  passphrase generation, room-code parser.
    79→    79→- `lib/session.ts` — memory-only room sessions (keys, password);
    80→    80→  refresh locks rooms.
    81→    81→- `lib/local.ts` — desk cards, verified marks, per-room settings
    82→    82→  (localStorage; metadata only, never secrets).
    83→    83→- `lib/relay.ts` — single shared socket via `NEXT_PUBLIC_RELAY_URL`.
    84→    84→- `lib/format.ts` — timestamps, TTL countdowns ("42s", "3d 04h").
    85→    85→- `store/app.ts` — the zustand orchestrator: hash router, room
    86→    86→  lifecycle, send/receive pipelines with signature verification, TTL
    87→    87→  scheduling, presence, unread dots.
    88→    88→
    89→    89→### 1.5 All screens (Tasks 4–5)
    90→    90→
    91→    91→- **S1 Landing** — one decision in five seconds: create, or join.
    92→    92→- **S2 Invite** — where a shared link lands; prefilled code,
    93→    93→  wrong-password neutral errors, "what happens next" disclosure.
    94→    94→- **S3 Sealing overlay** — the vault-press theatre while keys derive
    95→    95→  (minimum-duration theatre + real derivation).
    96→    96→- **S4 Room list** — the desk: locked cards (lock glyph), unlock
    97→    97→  sheet, ash state for burned rooms, unread dots.
    98→    98→- **S5 Chat** — header with a single shield glyph, grouped bubbles
    99→    99→  with corner rules, file cards, sealed/spent view-once cards,
   100→   100→  composer with TTL control + attach + view-once toggle +
   101→   101→  Reconnecting/Re-sealing states.
   102→   102→- **S6 Burn overlay** — char-in (2 s) → Lora line → fade; the room
   103→   103→  burns for everyone.
   104→   104→- **S7 Verification** — fingerprints computed from pubkeys, copy,
   105→   105→  verify marks, glossary.
   106→   106→- **S8 Empty states** — creator ("Invite someone to begin") and
   107→   107→  joiner ("You won't see messages from before you joined") variants.
   108→   108→- **S9 Settings** — local room name, default TTL, members, burn with
   109→   109→  a centered consequence dialog.
   110→   110→- PWA manifest + icon set.
   111→   111→
   112→   112→### 1.6 First QA round (Task 6) — nine bugs fixed
   113→   113→
   114→   114→1. `member:joined` broadcast lacked pubkey → receivers rejected all
   115→   115→   messages as "signature invalid" (relay now carries the JWK).
   116→   116→2. `rejoined` flag always true → "X returned." on first joins.
   117→   117→3. `message:ack` is room-scoped.
   118→   118→4. Composer TTL chip was cosmetic — the send path ignored it
   119→   119→   (`ttlOverride` param added).
   120→   120→5. zustand `?? []` selectors → `getSnapshot` infinite loop (stable
   121→   121→   `EMPTY` constants).
   122→   122→6. `creatorToken` was memory-only → creator couldn't burn after
   123→   123→   refresh (now device-local).
   124→   124→7. `#/` always redirected to rooms → landing unreachable (`#/new`).
   125→   125→8. Ash card never displayed — the sweep filtered burned cards on
   126→   126→   every call (sweep now once per page load).
   127→   127→9. Stale locked view after unlocking from a direct URL (chat screen
   128→   128→   subscribes to the messages slice).
   129→   129→
   130→   130→Verified end-to-end: two-member E2E with signature checks,
   131→   131→fingerprints matching across clients, view-once sealed → open →
   132→   132→spent-for-everyone, TTL countdown + burn choreography, mobile 390 px,
   133→   133→Nightfall, relay outage → "Reconnecting" → auto-reconnect + rejoin.
   134→   134→
   135→   135→### 1.7 Styling polish pass (Task 7-a, frontend-styling-expert)
   136→   136→
   137→   137→- Landing rhythm tightened; deliberate focus-visible rings (the
   138→   138→  global outline was invisible on forest-filled CTAs);
   139→   139→  `[@media(max-height:720px)]` compaction for short screens.
   140→   140→- Room cards: paper-lift hover (`hover:-translate-y-px`, no shadows),
   141→   141→  locked cards "sleep", unread dot seated on the x-height, ash cards
   142→   142→  get a quiet diagonal strikethrough.
   143→   143→- Composer: focus transitions, standardized disabled dimming, TTL
   144→   144→  chip hover borders, attachment slip restyle, 44 px hit areas.
   145→   145→- Bubbles: tabular-nums countdowns (no width jitter), stabilized
   146→   146→  sender-alias rows, system-line rhythm, file-card hovers.
   147→   147→- Chat header: real active states (bg-wash + scale-0.96), truncation
   148→   148→  with title tooltips.
   149→   149→
   150→   150→### 1.8 Deliverables (Task 8)
   151→   151→
   152→   152→- `DESIGN.md` written: token sheet, component inventory, motion spec,
   153→   153→  rationale lines, architecture record.
   154→   154→
   155→   155→---
   156→   156→
   157→   157→## 2. Feature rounds 1–3 (Tasks 9–17)
   158→   158→
   159→   159→### Round 1 (Tasks 9–12)
   160→   160→
   161→   161→- **Typing indicators**: relay `member:typing` broadcast (blind,
   162→   162→  transient); 3.5 s TTL with prune timers, 2.5 s emit throttle,
   163→   163→  cleared on message arrival; quiet ink-alias whisper line in a
   164→   164→  reserved 26 px strip ("Velvet Owl is writing…"), `aria-live=polite`,
   165→   165→  no layout jump.
   166→   166→- **Attachments three ways**: paperclip, drag-and-drop (dashed forest
   167→   167→  "Release to attach" overlay), clipboard paste into the textarea;
   168→   168→  chunked base64 (8 KB) for large files.
   169→   169→- **Time-gap dividers**: quiet centered timestamps between messages
   170→   170→  >30 min apart, `role=separator`.
   171→   171→- **Message length guard**: maxLength 4000 + near-limit counter
   172→   172→  (terracotta at zero, calm copy).
   173→   173→- **Sheet grabbers**: 36×4 mute handles on all mobile bottom sheets.
   174→   174→- Operational lesson recorded: `bun --hot` does **not** reliably
   175→   175→  reload socket.io handlers — the relay must be manually restarted
   176→   176→  after edits.
   177→   177→
   178→   178→### Round 2 (Tasks 13–15)
   179→   179→
   180→   180→- **Invite sheet restructure**: actions on the label row, full-width
   181→   181→  `break-all` mono value boxes — long passwords fit one line at
   182→   182→  390 px; revealed password gets `select-all`.
   183→   183→- **Scroll-to-bottom affordance**: quiet pill appears >220 px from
   184→   184→  the bottom with content overflow; shows "Latest" or "N new
   185→   185→  messages" (peers only); smooth scroll (auto under reduced motion).
   186→   186→- **Message copy context menu** (Radix ContextMenu): right-click
   187→   187→  desktop, press-and-hold mobile, quiet single-item paper-styled
   188→   188→  menu.
   189→   189→- **PWA service worker** (`public/sw.js` + `sw-register.tsx`):
   190→   190→  network-first navigations, stale-while-revalidate for static
   191→   191→  assets, **never caches `/api/` or socket.io** — ephemerality
   192→   192→  preserved; versioned caches cleaned on activate.
   193→   193→- Tailwind lesson recorded: custom `shadow-float` class loses to
   194→   194→  shadcn base `shadow-md` (utilities layer beats components) — use
   195→   195→  arbitrary values.
   196→   196→
   197→   197→### Round 3 (Tasks 16–17) — mobile keyboard bug
   198→   198→
   199→   199→**User-reported:** "on mobile, the keyboard covers the modal/bottom
   200→   200→popup." Root cause: iOS Safari overlays the keyboard over the layout
   201→   201→viewport; fixed-position sheets anchored to the un-resized ICB.
   202→   202→
   203→   203→- **New hook `use-keyboard-inset.ts`**: measures the keyboard via the
   204→   204→  visual-viewport formula, publishes `--kb-inset` (px) on `<html>`;
   205→   205→  rAF-batched; 0 px unless an editable is focused (no desktop/URL-bar
   206→   206→  false positives).
   207→   207→- `layout.tsx` viewport meta: `interactive-widget=resizes-content`
   208→   208→  (Android resizes natively; mechanisms never double-count).
   209→   209→- `ui/sheet.tsx` bottom variant: `bottom-[var(--kb-inset,0px)]` +
   210→   210→  `max-h-[calc(100dvh−kb-inset)]`, 250 ms transition; tall sheets
   211→   211→  scroll inside.
   212→   212→- App shell gets matching padding-bottom — the composer, typing
   213→   213→  strip, and message column rise above the keyboard.
   214→   214→- iOS input-zoom guard: 16 px font on coarse-pointer WebKit inputs
   215→   215→  (unlayered so it outranks utilities).
   216→   216→- QA simulated the exact on-device var (`--kb-inset: 301px` at
   217→   217→  390×844) and verified all three surfaces + blur reset + desktop
   218→   218→  no-op.
   219→   219→
   220→   220→---
   221→   221→
   222→   222→## 3. Protocol v2 — the security hardening pass (Task 19)
   223→   223→
   224→   224→The largest single change to the codebase, driven by an external
   225→   225→acceptance review. **All six property suites were written RED first.**
   226→   226→
   227→   227→- **Replay defense (P0)** — `lib/protocol.ts`: per-(sender, session)
   228→   228→  monotonic counters, ±10-minute timestamp window, frame-id dedup,
   229→   229→  FIFO cap; receive pipeline order fixed as shape → registry eviction
   230→   230→  → key version → signature → replay → dispatch. Watermarks persist
   231→   231→  per room per device (survive refresh).
   232→   232→- **Uniform padding (P1)**: every control frame is JSON → ECDSA sign
   233→   233→  → pad → AES-256-GCM at a uniform **20480+16 bytes** (20480 chosen
   234→   234→  because the 4000-char worst-case UTF-8 message cannot fit the
   235→   235→  spec's 4096 — uniformity is the property). File transfers: payload
   236→   236→  padded to a fixed 2.8 M base64 chars, always **exactly 45 uniform
   237→   237→  frames** (1 meta + 44 chunks) regardless of true size — the relay
   238→   238→  cannot distinguish typing from messages or read file sizes.
   239→   239→  **Typing became an encrypted uniform frame.**
   240→   240→- **KDF upgrade (P1)** — `lib/kdf.ts`: PBKDF2 → **argon2id**
   241→   241→  (m=64 MB, t=3, p=1, hash-wasm) with random 16-byte salt in a
   242→   242→  **versioned bundle** `{v:2,…}`; legacy PBKDF2 rooms still unlock.
   243→   243→- **Per-room identity (P1)** — `lib/room-identity.ts`: one device
   244→   244→  seed; per-room ECDSA keypair = HKDF(seed, roomId) — same device,
   245→   245→  two rooms ⇒ different pubkeys and aliases (registries cannot be
   246→   246→  correlated). Old global device key retired.
   247→   247→- **Real key rotation on leave (P0)** — `lib/room-protocol.ts`
   248→   248→  `RoomCipher`: the coordinator (lowest memberId among connected)
   249→   249→  generates a **random** 256-bit key (never password-derived),
   250→   250→  delivers it pairwise over authenticated ECDH, signed, sealed under
   251→   251→  the entry key — every member can read the offer, only the
   252→   252→  recipient can unwrap the key. The **server epoch is the rotation
   253→   253→  ledger** (persisted): post-refresh rotations re-seal past any key
   254→   254→  a departed member held; key requests self-heal through
   255→   255→  version-randomized fallback rotations that converge monotonically.
   256→   256→- **Relay/API hardening (P2)** — `lib/rate-limit.ts`: token bucket
   257→   257→  per socket, IP limit on room creation.
   258→   258→- **Adversarial review (subagent, read-only)** found and the round
   259→   259→  fixed: unauthenticated relay joins (registry poisoning → REST-
   260→   260→  authoritative writes), unauthenticated leave, a legacy spoiler
   261→   261→  frame, v1 downgrade in v2 rooms, missing key-request sender, kv
   262→   262→  saturation brick (KVERSION_CAP 1e6), unbounded maps (capped).
   263→   263→- Test count: **47/47**. Test count milestone: first 92-test run
   264→   264→  came later. Browser E2E covered rotation, rejoin key delivery,
   265→   265→  file transfer through 45 uniform frames with sha-verified
   266→   266→  assembly, and refresh-lock + argon2id unlock.
   267→   267→- `README.md` created, including the honest **"What CipherChat does
   268→   268→  NOT protect against"** section; DESIGN.md gained §6 threat model.
   269→   269→
   270→   270→---
   271→   271→
   272→   272→## 4. Round 20 — captions, ink reactions (Tasks 20-0…20-c)
   273→   273→
   274→   274→- **Bug fixed (found by QA)**: text typed alongside a file attachment
   275→   275→  was **silently dropped**. Fix: the caption rides the meta frame's
   276→   276→  canonical-signed top-level text field — exactly as unforgeable as a
   277→   277→  message; rendered above the file card; "Copy caption" added.
   278→   278→- **Ink reactions** (protocol → UI): four product-vocabulary marks —
   279→   279→  ✓ Acknowledged · ✦ Noted · ♥ Warmly received · ☾ Later. New
   280→   280→  `react` frame kind (glyph in the signed text field, target in
   281→   281→  `messageId`); one mark per sender per message; toggle transitions
   282→   282→  converge identically on every client; "Mark this message" context
   283→   283→  submenu + marks bar chips under bubbles with who-marked tooltips.
   284→   284→- **Escape-to-rooms**: in an active room with an empty composer,
   285→   285→  Escape navigates to the room list (sheets/menus/dialogs own the key
   286→   286→  first).
   287→   287→- **Service-worker dev guard**: SW no longer registers in development
   288→   288→  — closed the stale-chunk class of QA failures permanently.
   289→   289→- Styling pass: staggered landing entrance, presence ink dots in the
   290→   290→  header (per-member colours, away = 35% opacity), typing-line ink
   291→   291→  dots, jump-pill lift, hover micro-interactions.
   292→   292→
   293→   293→---
   294→   294→
   295→   295→## 5. Round 21 — silent-departure grace + UX (Tasks 21-0…21-4)
   296→   296→
   297→   297→- **Silent-departure grace** (the documented "silent leaver retains
   298→   298→  keys" gap, closed): relay stamps an offline clock on silent
   299→   299→  last-socket drops; clients watch a 120 s grace; the server
   300→   300→  **verifies against live presence before writing anyone out** (`POST
   301→   301→  /api/rooms/:id/evict` gathers DB registry + relay snapshot from a
   302→   302→  token-guarded internal presence endpoint on `:3004`); the epoch
   303→   303→  ledger makes the re-seal durable; the departed member re-enters
   304→   304→  with the password. "X drifted away." system line; honest-diff
   305→   305→  presence also fixed the stale away-dot after refresh.
   306→   306→- **Composer drafts** per room (memory-only like the keys): the draft
   307→   307→  swaps with the room, cleared on send/leave/burn; a soft
   308→   308→  forest-ring pulse when a draft restores.
   309→   309→- **Composer auto-focus** on room entry (desktop only) and after the
   310→   310→  invite sheet closes (won the focus race against Radix's trigger
   311→   311→  restoration).
   312→   312→- **Relay-offline strip**: "The line is down — letters pause until
   313→   313→  it returns." — strip-in/strip-out choreography, composer lock,
   314→   314→  auto-recovery verified with a full relay stop/start.
   315→   315→- **Unread counts**: persisted per room card, counts real letters
   316→   316→  only (joins/rejections stay dot-only), capped "99+", chip-pop
   317→   317→  entrance, reset on entry.
   318→   318→- **Two bugs caught by E2E before shipping**: an inverted
   319→   319→  `memberConnected` boolean that would have made the grace dead in
   320→   320→  production (the pure-function tests were right; only live E2E
   321→   321→  caught the adapter), and an init double-registration race
   322→   322→  (synchronous guard added).
   323→   323→
   324→   324→---
   325→   325→
   326→   326→## 6. Task 22 — security residuals, legal, brand, deploy
   327→   327→
   328→   328→- **22-b Security residuals** (test-first, +18 tests): `POST /leave`
   329→   329→  now requires **proof-of-possession** — an ECDSA signature from the
   330→   330→  member's own room key over `cc-leave-v1:{roomId}:{memberId}:{ts}`
   331→   331→  (±10 min window; idempotent replay; already-inactive leaves don't
   332→   332→  bump the epoch). Per-IP rate limits on room-info (30/min),
   333→   333→  verifier (30/min), member joins (12/min), leave (20/min). Member
   334→   334→  cap became a **freshness-windowed soft cap** (15 min) centralized
   335→   335→  in `lib/admission.ts`. HKDF scalar range made an explicit tested
   336→   336→  invariant (`[1, n−1]` bigint check). DESIGN.md gained the
   337→   337→  localStorage residue inventory.
   338→   338→- **22-c Legal package**: MIT `LICENSE`; `public/legal/terms.md` (18
   339→   339→  sections) + `privacy.md` (14 sections) — factually tied to the real
   340→   340→  architecture (no accounts, immutable password, E2EE moderation
   341→   341→  reality, no destruction warranty, self-hosting = you are the data
   342→   342→  controller); `legal-sheet.tsx` renders them in-app (markdown over
   343→   343→  react-markdown, paper-styled) from the landing footer and Settings
   344→   344→  → About.
   345→   345→- **22-d Brand mark (v1)**: "The Split Seal" — wax seal with a
   346→   346→  diagonal fracture; superseded in Task 23 by the final mark (below).
   347→   347→- **22-a/e/f Deploy/CI/env layer**: the app became sandbox-independent
   348→   348→  — `deploy/` (multi-stage `Dockerfile.web`, `Dockerfile.relay`,
   349→   349→  `docker-compose.yml` with web + relay + Caddy auto-TLS and a
   350→   350→  `handle_path /relay/*` prefix-strip proxy, db volume),
   351→   351→  `.env.example` as the configuration contract, `NEXT_PUBLIC_RELAY_URL`
   352→   352→  env-driven relay URL, `.github/workflows/ci.yml` (install → prisma
   353→   353→  generate → lint → tsc → 92 tests → npm audit → production build),
   354→   354→  service-worker update flow (v3: one persistent "A fresh seal is
   355→   355→  ready" toast with a Reload action; never auto-reloads — drafts
   356→   356→  live in memory), OG/Twitter card metadata.
   357→   357→- Full two-origin golden-path E2E on the dev servers, including the
   358→   358→  new departure proof, rejoin-after-rotation, view-once file with
   359→   359→  caption, and burn on both origins.
   360→   360→
   361→   361→---
   362→   362→
   363→   363→## 7. Task 23 — The Vanishing Ink, room TTL, message TTL
   364→   364→
   365→   365→- **Final brand mark**: the Split Seal was replaced by **The Vanishing
   366→   366→  Ink** — a hand-fallen ink drop whose top frays into three rising
   367→   367→  flecks, the last one ember-warm. `mark.tsx` exports `InkMark` with
   368→   368→  variants intact/scattered/fleck/ghost; ~15 call sites updated
   369→   369→  (typing dots are flecks, sender identities are flecks of the same
   370→   370→  ink in member colours, watermarks are drop ghosts); all PWA/apple/
   371→   371→  OG assets regenerated from the same geometry (`scripts/gen-icons.ts`
   372→   372→  is re-runnable).
   373→   373→- **Room TTL (creator-chosen)**: `Room.expiresAt` (nullable =
   374→   374→  until-burned); presets 24h/1h/7d/30d/Custom (5 min–30 d)/Until-
   375→   375→  burned; `PATCH /api/rooms/:id` (creatorToken-gated) adjusts later;
   376→   376→  lazy sweep of expired rooms on create; join/unlock surfaces say
   377→   377→  "This room's time ran out"; header countdown (terracotta under a
   378→   378→  minute); expiry watcher navigates out with a "Closed — its time ran
   379→   379→  out" ash-style card.
   380→   380→- **Message TTL (per message)**: presets gained 15s/30s/1m; custom
   381→   381→  editor (5 s–24 h, sec/min/hr units, live label); the composer's
   382→   382→  blind cycle button became a popover (desktop) / bottom sheet
   383→   383→  (mobile) with the same shared `TtlPicker`; messages burn with the
   384→   384→  ember choreography.
   385→   385→- `format.ts` learned sub-minute ("42s") and day-spanning ("3d 04h")
   386→   386→  countdowns.
   387→   387→
   388→   388→---
   389→   389→
   390→   390→## 8. Task 24 — the motion pass
   391→   391→
   392→   392→- **Hero choreography** (landing): the drop falls (580 ms gravity,
   393→   393→  squash at impact), the three flecks splash upward (staggered), a
   394→   394→  soft ink halo bleeds outward beneath, then the evaporation loop
   395→   395→  takes over. Two animations, disjoint CSS properties, one element —
   396→   396→  the trick that makes it composable.
   397→   397→- Ghost watermarks **draw themselves** (normalized stroke-dash
   398→   398→  draw-on, 1.3 s) then drift almost imperceptibly (13 s ±6 px).
   399→   399→- Screen transitions (280 ms fade + rise); chat screens keyed by
   400→   400→  roomId (switching letters is switching pages); messages arrive
   401→   401→  from the side they were written on (self → from the right margin,
   402→   402→  others → from the left, one soft overshoot).
   403→   403→- Typing whisper: the writer's ink flecks rise and fade on a loop.
   404→   404→- TTL countdowns breathe in their final ten seconds; two flecks
   405→   405→  (forest + ember) rise from the send button as a letter leaves;
   406→   406→  the theme icon swings in on change.
   407→   407→- **Regression found & fixed**: TTL rooms' entry system line had
   408→   408→  killed both empty states (`messages.length === 0` was never true) —
   409→   409→  empty now means "no letters", with system lines rendered above.
   410→   410→- `devIndicators:false` (the Next dev badge overlapped the composer
   411→   411→  on small screens); every animation degrades under
   412→   412→  `prefers-reduced-motion`.
   413→   413→
   414→   414→---
   415→   415→
   416→   416→## 9. Task 25 — notifications, overlap fix, mobile shell
   417→   417→
   418→   418→- **Notification engine** (`lib/notifications.ts`): preview
   419→   419→  preference content/sender/none (applies to both channels);
   420→   420→  channel decision — in the room → none, hidden/backgrounded →
   421→   421→  native, otherwise → in-app banner; truncated previews that never
   422→   422→  quote files; `setAppBadge`/`clearAppBadge` launcher badge sync.
   423→   423→- **The overlap fix, two layers**: (1) in-app banners are **in flow**
   424→   424→  — the app shell gained a sticky-top notice stack that opens a grid
   425→   425→  track (0fr→1fr, 360 ms) so the app *steps down*; nothing is ever
   426→   426→  covered. (2) Sonner toasts seat at `--toast-top = safe-area +
   427→   427→  64px + --cc-notice-h`, with a ResizeObserver publishing the
   428→   428→  banner height live — a toast can never land on the header or a
   429→   429→  banner.
   430→   430→- **NoticeStack + store/notices.ts**: one banner per room
   431→   431→  (replace-in-place, clock reset), max 3, 6.5 s auto-dismiss,
   432→   432→  two-step exit animation, focusable (Enter/Space → open room),
   433→   433→  `aria-live=polite`.
   434→   434→- **Native notifications round-trip**: `sw.js` gained
   435→   435→  `notificationclick` (focus existing client + postMessage
   436→   436→  navigation, else open the room deep link); `sw-register.tsx`
   437→   437→  routes the navigation in dev too while registration stays
   438→   438→  production-only.
   439→   439→- **App settings sheet**: a Notifications section walking all four
   440→   440→  permission states (Allow button / 3-option preview radio / 
   441→   441→  re-enable copy / banners-still-work copy) + an Install section
   442→   442→  (beforeinstallprompt captured; iOS 3-tap walkthrough).
   443→   443→- **PWA/manifest**: `display_override`, `launch_handler:
   444→   444→  focus-existing`, app shortcuts (New room / Your rooms).
   445→   445→- **iOS splash screens**: 13 per-resolution launch PNGs (105 KB
   446→   446→  total) generated by `scripts/gen-splash.ts` — the mark geometry is
   447→   447→  read from `logo.svg` at generation time (never re-drawn), and the
   448→   448→  composition positions are set by alpha-channel measurement, not
   449→   449→  eyeballing. `apple-splash.tsx` serves the links.
   450→   450→- **Capacitor scaffold**: `capacitor.config.ts` (remote-URL mode —
   451→   451→  the shell frames the deployed origin; TLS only) + `MOBILE.md`
   452→   452→  (PWA install path, the full `npx cap` pipeline, honest limits
   453→   453→  including why there is no web push by architecture, and the 4-step
   454→   454→  VAPID future-work sketch).
   455→   455→- Test count: **108/108** (16 new notification cases).
   456→   456→
   457→   457→---
   458→   458→
   459→   459→## 10. Task 26 — the password breach (user challenge)
   460→   460→
   461→   461→User pasted the live DOM of the create-sheet password input
   462→   462→(`type="text"`, `value="quill-grove-harbor-orchard"`) and asked to
   463→   463→find the breach. Three layers found and fixed:
   464→   464→
   465→   465→1. **CRITICAL — the generator drew from `Math.random()`** (V8
   466→   466→   xorshift128+, predictable). `lib/identity.ts` rewritten: a
   467→   467→   **256-word list** in the app's stationery/nature voice, sampled
   468→   468→   via `crypto.getRandomValues` with rejection-guarded unbiased
   469→   469→   sampling (one byte = one word at list size 256, zero modulo
   470→   470→   bias; duplicates skipped).
   471→   471→2. **CRITICAL — 18 bits of keyspace** (24 words, 4 picks). Raised to
   472→   472→   **5 words = 2^40 candidates** — the public verifier oracle moves
   473→   473→   from an afternoon to a multi-year grind through argon2id.
   474→   474→3. **Input hygiene**: `autocomplete="off"` → `"new-password"` (the
   475→   475→   documented suppressor of Chrome's save-password prompt for
   476→   476→   ephemeral room passwords); the reveal toggle became
   477→   477→   keyboard-focusable with `aria-pressed`; plaintext state now
   478→   478→   clears the moment its job is done (create sheet, join, unlock,
   479→   479→   invite). The memory-only session retention stays, documented and
   480→   480→   deliberate (the invite sheet re-shares the password; legacy v1
   481→   481→   epoch re-derivation needs it).
   482→   482→- DESIGN.md gained the "Room passwords" bullet stating all of this,
   483→   483→  including the inherent limit.
   484→   484→
   485→   485→---
   486→   486→
   487→   487→## 11. Task 27 — the DevTools value-attribute fix (user challenge)
   488→   488→
   489→   489→User: *"while typing using DevTools, in sensitive fields like
   490→   490→passwords, the `value=""` tag always reveals it. Can't that be
   491→   491→hidden or removed?"*
   492→   492→
   493→   493→- **Diagnosis**: React controlled inputs mirror every keystroke into
   494→   494→  the DOM `value` **attribute** (visible in Elements), and the
   495→   495→  plaintext also sat in React state (readable in React DevTools).
   496→   496→- **Fix — `PasswordField` became the uncontrolled secret field**:
   497→   497→  props now `Omit<InputHTMLAttributes, "value" | "defaultValue">` —
   498→   498→  a **compile error** to pass either. The attribute is never
   499→   499→  written; no plaintext ever sits in React state. Parents
   500→   500→  seed/wipe imperatively through the forwarded ref (`el.value = …`
   501→   501→  sets the property only) and read once at submit.
   502→   502→- **All four consumers migrated**: create sheet (landing), join form
   503→   503→  (invite), locked-room view (chat), unlock sheet (room-list) — each
   504→   504→  reads the ref at submit and wipes on success/close/unmount.
   505→   505→- **Subtle bug found during QA and fixed**: the create-sheet seed
   506→   506→  effect initially lived on the parent — but Radix `Presence` mounts
   507→   507→  portal children in a **later commit** than the parent's open-state
   508→   508→  change, so it fired before the input existed. Fix: `SeedPassphrase`,
   509→   509→  a render-null component *inside* the portal content whose mount
   510→   510→  effect seeds the field exactly when it comes to life (and re-runs
   511→   511→  on every reopen).
   512→   512→- **Unrelated real bug found & fixed — the permanent offline state**
   513→   513→  visible in the user's screenshot: `.env` was missing
   514→   514→  `NEXT_PUBLIC_RELAY_URL`, so clients fell back to `/relay/`, which
   515→   515→  the sandbox gateway does not forward → every room showed "The
   516→   516→  line is down" forever. `.env` now carries the sandbox value
   517→   517→  `/?XTransformPort=3003`; verified live (composer enables,
   518→   518→  messages relay end-to-end).
   519→   519→- Verified: `getAttribute('value') === null` at every step of every
   520→   520→  flow — seeded, toggled, typed-into, reseeded, submitted, unlocked.
   521→   521→  The honest limit stands in DESIGN.md: the DOM `value` *property*
   522→   522→  is always readable from the user's own machine — that is true of
   523→   523→  every site, and the page must read the secret to encrypt with it.
   524→   524→
   525→   525→---
   526→   526→
   527→   527→## 12. Round 29 — quoted replies + in-app media viewers
   528→   528→
   529→   529→Two features landed together because they touch the same seam: what
   530→   530→a message can point at, and what opening a message means.
   531→   531→
   532→   532→### 12.1 Quoted replies to specific messages
   533→   533→
   534→   534→- **`src/lib/types.ts`** — new `ReplySnapshot` interface
   535→   535→  `{id, senderId, snippet, file?}`; `REPLY_SNIPPET_MAX = 120`;
   536→   536→  `makeReplySnapshot()` builds the quote at reply-compose time — **view-once
   537→   537→targets are quoted as "Sealed message"**, never by their contents (the
   538→   538→sealed card's own rule: nothing shows before opening); file targets quote
   539→   539→the file's name and carry `file: true`; text targets are
   540→   540→whitespace-collapsed and capped at the first 120 characters.
   541→   541→  `sanitizeReplySnapshot()` is the post-verification UI gate (id/senderId
   542→   542→capped at 64 chars, snippet at 160, malformed shapes dropped — a failing
   543→   543→snapshot renders the message unquoted, never broken);
   544→   544→  `isReplySnapshot()` is the shape guard used on both sealing and
   545→   545→verification. `MessageView` gained `replyTo?: ReplySnapshot`.
   546→   546→- **`src/lib/protocol.ts`** — `FrameBody.reply?: ReplySnapshot`; new exported
   547→   547→  `replyCanonical()` serialises the snapshot deterministically
   548→   548→  (unit-separator-joined slots — the separator is cosmetic, slot order is
   549→   549→  what binds); **`canonicalV2` now appends the replyCanonical component as a
   550→   550→  12th field** — the quote is signature-covered, so a quote is exactly as
   551→   551→  unforgeable as the words it carries; a tampered quote breaks the ECDSA
   552→   552→  signature. `sealFrame` extracts and includes it.
   553→   553→- **`src/lib/room-protocol.ts`** — `sealText`/`sealFile` accept `reply`; the
   554→   554→  file-assembly map stores it; the completed-file OpenResult carries it;
   555→   555→  and `open()` verifies with the **raw** reply before any sanitising —
   556→   556→  verification must recompute the sender's exact canonical string, not a
   557→   557→  cleaned-up one.
   558→   558→- **`src/store/app.ts`** — `sendMessage(text, file?, ttlOverride?, reply?)`;
   559→   559→  the optimistic view carries `replyTo`; both receive paths (text frames and
   560→   560→  file-assembly completion) sanitise for the view, after verification.
   561→   561→- **UI, `src/components/cc/bubble.tsx`** — `QuoteBlock`: ink-coloured left
   562→   562→  border in the **quoted** sender's ink (`var(--ink-N)`, "Someone" in mute
   563→   563→  once they leave), alias + snippet, a `FileText` glyph for file quotes;
   564→   564→  click → jump to the original (`scrollIntoView` center) with a
   565→   565→  `quote-flash` soft forest pulse in `globals.css` (1300ms, once;
   566→   566→  reduced-motion: static 8% tint); the honest toast "That message is no
   567→   567→  longer in this session" when the original burned or expired. Reply
   568→   568→  affordances, three roads to one verb: the context menu's "Reply" item
   569→   569→  (press-and-hold on touch), a hover-only reply icon button beside the
   570→   570→  bubble on desktop (`group/row` opacity transition, `-left-10` for self /
   571→   571→  `-right-10` for others), and double-click on the bubble. Every message row
   572→   572→  carries `data-mid` for jump targeting.
   573→   573→- **UI, `src/components/cc/composer.tsx`** — the reply slip above the input
   574→   574→  (quote + X to cancel); the input is focused when a reply begins (touch
   575→   575→  included — the reply IS an intent to write); submit seals the snapshot
   576→   576→  into the message; **Escape while replying cancels the quote first**, with
   577→   577→  `stopPropagation` so the window-level escape-to-rooms handler doesn't
   578→   578→  fire.
   579→   579→- **UI, `src/components/screens/chat.tsx`** — `replyTarget` state (cleared on
   580→   580→  room switch and when the target burns — render-time adjustment pattern);
   581→   581→  `jumpToMessage` with `CSS.escape` + flash restart (class remove → reflow →
   582→   582→  add, so repeated jumps re-announce); viewer and reply both follow their
   583→   583→  message out when it burns.
   584→   584→- **Honest failure mode, chosen deliberately**: a mixed-version session — an
   585→   585→  old tab running pre-reply code beside a new one — will **drop** new reply
   586→   586→  frames rather than accept a quote it cannot verify. The letter is lost
   587→   587→  for that tab, never forged.
   588→   588→
   589→   589→### 12.2 In-app viewers for ALL media (no forced downloads)
   590→   590→
   591→   591→- **`src/components/cc/file-viewer.tsx` REWRITTEN** — exported
   592→   592→  `classifyFile(mime, name)` routes by mime + extension to
   593→   593→  image | video | audio | pdf | text | csv | binary. All bytes decode once
   594→   594→  to memory and render through **revocable blob: URLs** — created in an
   595→   595→  effect keyed on the message id, revoked on switch/unmount. Nothing is
   596→   596→  fetched; nothing is written.
   597→   597→- **Viewers per type**: images with click-zoom (unchanged behaviour, now fed
   598→   598→  from the blob); `<video controls playsInline
   599→   599→  controlsList="nodownload noremoteplayback">` (PiP disabled and contextmenu
   600→   600→  suppressed for view-once); a styled `<audio>` card with the same
   601→   601→  controlsList; **PDFs render via pdfjs-dist v6 onto a `<canvas>`**
   602→   602→  (`bun add pdfjs-dist`; worker copied to `public/pdf.worker.min.mjs`,
   603→   603→  served from `/pdf.worker.min.mjs`) with devicePixelRatio scaling (capped
   604→   604→  2×), fit-width, Back/Next page nav and "Page x of y" — canvas rendering
   605→   605→  means **no browser PDF toolbar, no save button**. pdf.js transfers
   606→   606→  buffers, so it is handed `bytes.slice()` — a copy. If pdf.js fails to
   607→   607→  load: regular files fall back to an `<iframe>` with the blob URL
   608→   608→  (honest), but **view-once files STAY SEALED** — falling back would expose
   609→   609→  the browser PDF toolbar's save button, and the notice says so. Corrupt
   610→   610→  PDFs set a failed state rather than showing a blank canvas. Text files
   611→   611→  render as escaped plain text in a monospace `pre` (React escaping — never
   612→   612→  innerHTML) with a wrap toggle and a 100 KB display cap
   613→   613→  (`TEXT_SHOW_MAX = 100_000`; "copy takes the whole file"); HTML is labelled
   614→   614→  "Shown as source — HTML is never executed here"; **SVG only ever rides an
   615→   615→  `<img>`**, the context where its scripts cannot run. CSV parses via a
   616→   616→  quote-aware RFC-4180-style `parseCsv()` (pure, exported, tested) into a
   617→   617→  sticky-header table capped at 500 rows × 32 cols (`CSV_MAX_ROWS` /
   618→   618→  `CSV_MAX_COLS`). Unknown binaries get a metadata card + a hex dump of the
   619→   619→  first 512 bytes.
   620→   620→- **Header & buttons** — name · size · mime; Copy contents (text-ish — the
   621→   621→  whole file); Download, present ONLY for non-view-once files; Close.
   622→   622→  **View-once files have NO download anywhere** — the explicit note reads
   623→   623→  "View once — it lives on screen only. There is no download for this file,
   624→   624→  from anyone, by design."
   625→   625→- **`src/components/cc/bubble.tsx` FileContent** — ALL non-view-once file
   626→   626→  cards now OPEN THE VIEWER (Eye glyph) instead of downloading directly;
   627→   627→  video cards (Film icon) and audio cards (Music icon) carry kind labels;
   628→   628→  download is a choice inside the viewer, never the card's whole job.
   629→   629→- **`src/components/screens/chat.tsx`** — the viewer closes when the message
   630→   630→  burns while open (render-time adjustment).
   631→   631→- **`eslint.config.mjs`** — `"public/**"` added to ignores (the pdf worker
   632→   632→  is a vendored minified asset, not our code to lint).
   633→   633→
   634→   634→### 12.3 Tests and QA
   635→   635→
   636→   636→- **Tests** (`src/lib/__tests__/task-29-replies.test.ts`, 16 new cases;
   637→   637→  suite now **124**): reply round-trip on text and through file assembly;
   638→   638→  a relabeled frame is rejected; a reply frame is size-indistinguishable
   639→   639→  from plain text (uniform `CONTROL_FRAME_BYTES`); replay refusal;
   640→   640→  `replyCanonical` determinism and shape-guarding; a direct ECDSA proof
   641→   641→  that changing the quote breaks the signature; `makeReplySnapshot`
   642→   642→  (truncation, file label, sealed-for-view-once); `sanitizeReplySnapshot`
   643→   643→  caps/drops; `classifyFile` routing; `parseCsv` (quotes, commas,
   644→   644→  newlines, CRLF, single-column).
   645→   645→- **E2E QA (verified, two browser sessions through the gateway
   646→   646→  127.0.0.1:81, room CMJ1NT8144)**: reply sent via the hover button — quote
   647→   647→  block rendered on both sides with the correct alias/snippet; quote click
   648→   648→  jumped + flashed; context-menu Reply worked; Escape cancelled the reply
   649→   649→  without leaving the room; a reply to a spent sealed message quoted
   650→   650→  "Sealed message". Viewers: text (pre + copy + download), CSV table
   651→   651→  (header + 3 rows), audio (blob src + nodownload controlsList), video (a
   652→   652→  real 2s mp4 played, readyState 4), PDF rendered on canvas at desktop and
   653→   653→  390px mobile ("Page 1 of 1", ~563 text pixels painted), and a
   654→   654→  deliberately corrupt PDF fell back to the iframe (non-view-once path
   655→   655→  verified). View-once file: opened with content, NO download button, the
   656→   656→  by-design note present, and the spent card propagated to both sessions.
   657→   657→  Consoles clean (in-page error listeners: zero errors), dev.log all
   658→   658→  200/201. VLM review of the desktop room, video viewer and mobile PDF
   659→   659→  screenshots: clean. tsc clean, eslint 0 errors, 124/124 tests.
   660→   660→
   661→   661→---
   662→   662→
   663→   663→## 13. Current state inventory
   664→   664→
   665→   665→**Tests:** 124/124 across 14 files (replay, rotation, kdf, identity,
   666→   666→padding, hardening, captions, reactions, silent-grace, leave-proof,
   667→   667→identity-range, admission, notifications, replies + viewer/CSV parsing).
   668→   668→Lint clean; tsc clean in src/.
   669→   669→
   670→   670→**Services:** Next.js dev `:3000` · relay `:3003` (socket.io,
   671→   671→in-memory, supervised) · presence snapshot `:3004` (token-guarded) ·
   672→   672→gateway `:81` (Caddy, `XTransformPort` routing).
   673→   673→
   674→   674→**Docs:** `README.md` (product + honest NOT-protect list + running
   675→   675→it) · `DESIGN.md` (tokens, motion vocabulary, architecture §5,
   676→   676→threat model §6) · `COMPONENTS.md` (full component/behavior
   677→   677→reference) · `MOBILE.md` (PWA + Capacitor + honest limits) ·
   678→   678→`LICENSE` + `public/legal/` (MIT, Terms, Privacy) · `worklog.md`
   679→   679→(this history's raw source).
   680→   680→
   681→   681→**Deploy:** `deploy/` (docker-compose: web + relay + Caddy TLS,
   682→   682→`.env.example` contract) · `.github/workflows/ci.yml`.
   683→   683→
   684→   684→**Known residuals (documented, not hidden):** compose-build E2E needs
   685→   685→a Docker host; first real CI run pending; `next.config.ts`
   686→   686→`typescript.ignoreBuildErrors` still true (CI's explicit tsc is the
   687→   687→real gate); no web push by architecture (VAPID sketched in MOBILE.md
   688→   688→with its privacy price); evict trusts the relay's in-memory clock;
   689→   689→nuisance-eviction residual; replay-to-fresh-device within 10 min;
   690→   690→5-word passphrase could grow to 6–7 for paranoid rooms.
   691→   691→