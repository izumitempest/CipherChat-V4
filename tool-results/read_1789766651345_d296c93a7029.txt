   337→  - REST POST /api/rooms/:id/evict: gathers facts (DB registry + relay snapshot via 3s-timeout server-to-server fetch), obeys authorizeEviction, deactivates the target + bumps the epoch (rotation ledger), 30/min/IP. Server-side matrix verified with curl: self→409 "self", fake caller→409 "caller-unknown", real eviction→200 + epoch bump, re-evict→409 "target-unknown" (idempotent). Unknown rooms answer ok:true (existence not confirmed to strangers).
   338→  - Store: silentGrace timer map armed by member:presence(false) AND the room:state honest-diff (registry members absent from the relay's live list are marked offline — also fixes the pre-existing stale away-dot after refresh); fireSilentEviction (only the still-connected coordinator acts) → POST /evict → member:expired announce → confirmDeparture() locally. member:left and member:expired now share confirmDeparture() (REST-confirmed eviction, "X left." vs "X drifted away.", coordinator rotation, "The room re-sealed for those who remain."). Grace clocks cancel on any return; clear on leave/burn/relay-disconnect.
   339→  - E2E (two origins): B closes the tab silently → 120s grace → A's room shows "Ashen Wren drifted away." + "The room re-sealed for those who remain." → DB: B inactive, epoch 2 → B returns with the password → key delivered (kv2) → bidirectional messaging under the rotated key. The pre-eviction curl tests + unit tests cover too-soon/target-online; the invariant test asserts client grace > server floor.
   340→- 21-2 FEATURES:
   341→  a) Per-room composer drafts (lib/drafts.ts, memory-only like the keys): the draft swaps with the room (render-time adjustment — lint's set-state-in-effect rule forced the sanctioned pattern), cleared on send/leave/burn. E2E: draft typed in Alpha → switch to Beta (fresh) → back to Alpha → draft restored + composer focused.
   342→  b) Composer auto-focus on room entry (desktop only — touch keyboards stay closed) + after the invite sheet closes (Radix onCloseAutoFocus preventDefault → composer.focus(); the naive effect lost the focus race to Radix's trigger restoration).
   343→  c) Relay-offline strip in the chat: "The line is down — letters pause until it returns." (role=status, terracotta) — E2E: relay fully stopped → strip + composer lock after the ping-timeout window → relay restarted → strip lifts, composer re-enables, both members auto-rejoined, post-recovery message delivered.
   344→  d) Unread counts: RoomCard.unreadCount (persisted number — metadata only), touchCard(roomId, unread, letters) counts real letters (text/file/legacy renders; joins and rejections stay dot-only), capped 99, reset on room entry. E2E: A at the desk, B sends 2 → sidebar chip "2" (aria "2 unread messages"), dot correctly absent.
   345→- 21-3 STYLING (frontend-styling-expert; see its section above — verified and kept after its context deadline).
   346→- 21-4 E2E + fixes found BY the E2E (the round's real wins):
   347→  - **CRITICAL wiring bug caught**: fireSilentEviction passed memberConnected as the RESULT of comparing (true when offline) instead of the member's connected STATE (false when offline) — inverted semantics meant stillSilentAtExpiry would NEVER pass: the grace could never fire in production. The pure function's 16 tests were correct; only live E2E caught the adapter. Fixed + commented; the fast-probe technique (temp 10s grace + 5s floor, console probes, clean reloads) found it in minutes.
   348→  - **Latent double-registration bug**: init()'s ready-guard has an await gap — two concurrent init() calls (remount racing mount) register every relay handler twice (observed as duplicate member:presence processing). Fixed with a synchronous initStarted flag before the first await.
   349→  - QA-process rule learned the hard way (three times): **never edit code mid-E2E** — Fast Refresh re-evaluates the store module and leaves open tabs with stale sockets/duplicate handlers/stuck relayOnline. Any edit ⇒ reload every tab before continuing. Also: background tabs drop focus (press Enter fails) — send via the Send button or eval+input event; two-member QA needs two ORIGINS (localhost:81 vs 127.0.0.1:81).
   350→  - VLM review of 3 screenshots (light chat, desk, dark chat): CLEAN ×3.
   351→  - Docs: README (grace row in the property table + test counts 74 + the NOT-protect list now says "closed by the grace"), DESIGN.md §6 threat model (silent leavers bounded; nuisance-evicter residual documented), COMPONENTS.md (round-21 internals, lib table, backend surface incl. :3004 snapshot).
   352→
   353→Stage Summary:
   354→- 74/74 vitest green (58 + 16 silent-grace), tsc src clean, lint clean, dev.log clean, relay :3003 + snapshot :3004 supervised and token-armed.
   355→- The silent-leaver gap is closed end-to-end: relay stamps the disconnect clock, clients watch a 120s grace, the server verifies against live presence before writing anyone out, the epoch ledger makes the re-seal durable, and the departed member simply re-enters with the password.
   356→- Features: drafts, auto-focus, relay-offline strip, unread counts. Styling: strip choreography, chip pop/beat, draft-restored ring (subagent pass verified green).
   357→- Two real bugs the E2E caught before they could ship: the inverted memberConnected wiring (grace-dead) and the init double-registration.
   358→
   359→Unresolved issues / risks:
   360→- The evict route trusts the relay's in-memory clock; a relay restart mid-grace loses offlineSince and falls back to the DB's lastSeenAt (join time) — honest but coarser (documented in the route).
   361→- A nuisance-evicter who knows only the roomId can force a rotation while a member is away (same accepted-risk class as insider nuisance-rotations; the member re-enters with the password) — documented in DESIGN.md.
   362→- The relay strip appears only after the client's ping-timeout detection (~60-85s worst case through the gateway); sub-second relay bounces are often invisible (correct, quiet behavior).
   363→- member:presence/member:expired remain unauthenticated at the relay (rate-limited) — safe because every consumer REST-confirms; unchanged from 19-10 posture.
   364→- QA gotcha for future rounds (now triple-confirmed): reload ALL tabs after ANY code edit mid-QA; service worker dev-guard from round 20 held throughout (swCount 0).
   365→
   366→Priority recommendations for next phase:
   367→- Relay join member token (server-issued auth on room:join — removes the REST confirmation round-trip and the last unauthenticated relay ingress).
   368→- Optional polish candidates: burn-room countdown affordance in the header, member list sheet (who's who + fingerprints in one place), room-list skeleton shimmer (still low value).
   369→- If another hardening round: TLS-like handshake transcript binding for key offers; presence-driven rotation could gain a per-room grace override in settings.
   370→- Operational reminders: manual relay restart after mini-service edits (supervisor + `bun run dev` now carries --env-file; double-fork `setsid` pattern required to survive the tool session); two-member QA needs two ORIGINS.
   371→
   372→---
   373→Task ID: 22-b
   374→Agent: general-purpose (security residuals)
   375→Task: Security residuals hardening — proof-of-possession for leave, per-IP rate limits on the registry endpoints, freshness-windowed member cap, explicit scalar-range handling in room identity derivation, DESIGN.md threat-model updates (test-first).
   376→
   377→Work Log:
   378→- Wrote the three property suites RED first (18 tests): task-22.1-leave-proof (9), task-22.2-identity-range (5), task-22.3-admission (4) — all named after the property they protect, matching the task-19 style.
   379→- lib/leave-proof.ts (new): LEAVE_PROOF_TOLERANCE_MS = 10 min; leaveProofInput → exact canonical `cc-leave-v1:${roomId}:${memberId}:${ts}`; signLeaveProof/verifyLeaveProof reuse crypto.ts signCanonical/verifyCanonical (WebCrypto ECDSA P-256 SHA-256, standard base64). verify returns false — never throws — for non-JWK pubkeys, malformed b64, wrong signatures, non-finite ts, and |now − ts| > tolerance; boundary at exactly ±10 min still verifies (test-asserted). Replay of a valid {ts,sig} still verifies — acceptable and documented in the test: leaving is idempotent, and nuisance rotation requires the victim's own valid signature.
   380→- room-protocol.ts: public RoomCipher.signDepartureProof(ts = Date.now()) signing (this.roomId, this.selfId) with this.sigPrivJwk, placed next to sealBurn in the signing section.
   381→- leave route: body {memberId, ts, sig} all required → 400 "invalid-proof"; unknown room/burned/missing member keeps the old ok:true semantics (existence not confirmed to strangers); member.pubkey JSON-parsed and verified via verifyLeaveProof → 403 "bad-proof" on failure. Module-level IpRateLimiter 20/min/IP with a locally-copied clientIp helper. DEVIATION (deliberate): on verify success a member row that is ALREADY inactive no-ops without bumping the epoch — makes the specified replay comment literally true ("bumps epoch at most once per window") and closes a 10-minute replay-rotation nuisance; the first valid leave behaves exactly as before (mark inactive, bump epoch, return {ok, epoch}).
   382→- store/app.ts leaveRoom: getCipher(roomId) BEFORE dropping anything; with a cipher it awaits signDepartureProof() and POSTs {memberId, ts, sig}; with NO cipher (locked/refreshed tab) the REST call is skipped entirely (the server would 400 it anyway) and the relay member:leave announce still fires — receivers handle a keyless departure via the silent-eviction grace path (commented in place).
   383→- lib/rate-limit.ts: added ROOM_INFO_PER_MIN = 30, MEMBER_JOIN_PER_MIN = 12, MEMBER_FRESH_WINDOW_MS = 15 min (documented).
   384→- [roomId]/route.ts (GET room info) and [roomId]/verifier/route.ts (PUT): each gained its own module-level IpRateLimiter (ROOM_INFO_PER_MIN) → 429 "slow-down" on excess; clientIp copied locally per file; all existing behavior otherwise preserved.
   385→- lib/admission.ts (new): ROOM_MEMBER_CAP moved here (12); decideAdmission — existingMemberId → "rejoin" (always, even when full), recentlySeenCount >= cap → "full", else "admit"; cap override supported. rooms/route.ts re-exports ROOM_MEMBER_CAP from @/lib/admission (old import sites keep working); [roomId]/route.ts and members/route.ts updated to import from @/lib/admission directly.
   386→- members route POST: per-IP join limiter (MEMBER_JOIN_PER_MIN) → 429; inline cap check replaced by decideAdmission over a count of active members with lastSeenAt within MEMBER_FRESH_WINDOW_MS, with an honest comment on the trade-off (freshness-windowed SOFT cap: stops a code-holder without the password from permanently locking the room with throwaway keys; coordinated many-IP attackers can still exceed it — presence noise, not a confidentiality issue, per the Task 19 acceptance review). Rejoin/upsert path untouched.
   387→- lib/room-identity.ts: added bytesToBigIntBE + exported scalarToRoomIdentity(scalar) → RoomSigningIdentity | null with an EXPLICIT range check (all-zero → null; bigint value >= curve order → null), same JWK/fingerprint construction including the WebCrypto importKey proof; deriveRoomSigningKey now just retries on null (behavior/determinism identical for valid scalars — verified by the existing task-19.4 suite plus the new range tests). TWO FACT CORRECTIONS vs the task brief: @noble/curves v2.4.0 exposes the order as p256.Point.CURVE().n (p256.CURVE.n is undefined in this version), and the importKey proof forces the function async (Promise<RoomSigningIdentity | null>). BigInt() constructor calls instead of literals — the repo targets ES2017 (tsconfig owned by another agent, untouched).
   388→- DESIGN.md §6: added the missing localStorage residue inventory as a "What lingers on your device" bullet (room cards = metadata, creator tokens = burn authority, verification marks, per-room replay watermarks = seen-message counters for replay protection — non-secret residue that never leaves the device; keys/messages memory-only), plus the "Residuals closed in Task 22" note (leave requires proof-of-possession; room-info/verifier/members per-IP rate-limited; freshness-windowed soft cap with documented DoS residual). §5 server line updated to enumerate all per-IP limits.
   389→- Verification: bunx vitest run → 92/92 green (74 pre-existing + 18 new, zero regressions); bun run lint → clean; bunx tsc --noEmit → zero errors in src/ (only the pre-existing examples/ + skills/ errors the spec filters out); dev.log clean, no service restarts.
   390→
   391→Stage Summary:
   392→- The unauthenticated POST /leave residual flagged by the Task 19-10 adversarial review ([P1]) is closed: a leave now requires a valid ECDSA signature from the member's own room key, idempotent on replay, rate-limited 20/min/IP.
   393→- The existence/epoch oracle (GET room info, PUT verifier) and the registry write path (member join) are per-IP rate-limited (30/min and 12/min); the member cap is now a documented freshness-windowed soft cap with the policy centralized and property-tested in lib/admission.ts.
   394→- Room identity derivation's scalar validity is an explicit, testable invariant ([1, n-1] checked as bigint) instead of a library throw.
   395→- Test counts: 92 passed of 92 across 12 files. Files: created lib/leave-proof.ts, lib/admission.ts, three test files; edited room-protocol.ts, room-identity.ts, rate-limit.ts, app.ts (store), leave/route.ts, members/route.ts, [roomId]/route.ts, verifier/route.ts, rooms/route.ts, DESIGN.md.
   396→- Behavior change the orchestrator must know: leaveRoom only performs the REST leave when a live cipher exists (unlocked room); a locked/refreshed tab skips REST and relies on the relay announce + silent-eviction grace, and a second valid leave for an already-inactive member no longer bumps the epoch.
   397→
   398→---
   399→Task ID: 22-c
   400→Agent: general-purpose (legal)
   401→Task: Legal work package — MIT LICENSE, Terms of Use + Privacy Policy (single source of truth in /public/legal), and their in-app presentation (legal-sheet.tsx wired into landing footer, Settings "About", and the app shell).
   402→
   403→Work Log:
   404→- Read worklog.md (Tasks 0–21) and DESIGN.md in full; cross-checked every claim in the documents against the real architecture (argon2id 64MB in-browser KDF, AES-256-GCM uniform padding, per-room ECDSA P-256, memory-only history, blind server inventory, relay stdout lines, silent-leaver grace, ±10-min replay residual, 12-member freshness-windowed soft cap, view-once-as-promise, immutable password).
   405→- Created LICENSE — standard MIT, "Copyright (c) 2026 CipherChat Contributors".
   406→- Created public/legal/terms.md (18 numbered sections, ~2,000 words): what CipherChat is/is not (not a hosted service provider; reference deployment), acceptance, eligibility (no under-13s; guardian consent), no-account/no-recovery model + immutable password, acceptable use, content-moderation reality under E2EE (operator cannot read ⇒ cannot moderate; no report button by design), ephemeral nature with NO destruction warranty (memory of other members, screenshots, view-once limits), self-hosting rights/duties under MIT, IP (user content stays the user's), AS-IS warranty disclaimer, liability cap at zero, indemnification, no SLA, export-controls notice, changes (no accounts ⇒ no notification channel), termination, "[Your Jurisdiction]" governing-law placeholder + no-governing-entity reference deployment, contact placeholder.
   407→- Created public/legal/privacy.md (14 numbered sections, ~1,800 words): privacy architecture; the precise server inventory (room id, unreadable verifier blob, creator token, epoch counter, member registry fields); what never exists anywhere (content at rest, passwords, cross-room identities, analytics/cookies/third parties/ads); the full localStorage inventory with the "passwords and decrypted content are never written to disk" guarantee; how to delete everything (burn / clear site data + the honest limit); operator stdout join/leave lines; in-memory rate-limit counters; children; data security with the full honest residuals list; self-hosting = you are the data controller (upstream collects nothing); international-transfer note for self-hosters; policy changes; contact placeholder. Both docs reference "the published threat model in the repository (DESIGN.md)" and use calm, non-marketing language.
   408→- Created src/components/cc/legal-sheet.tsx ("use client"): local zustand store useLegalSheet (open: "terms" | "privacy" | null, show, close); LegalLinks (two inline forest links, underline offset, hover states, keyboard-focusable, 150ms reduced-motion-safe color transitions, wraps at 360px); LegalSheetHost renders the open doc in the existing shadcn Sheet primitive (side bottom/right like SettingsSheet — centered modals are reserved for burn per DESIGN §4/file-viewer; near-full-screen h-[92dvh] on mobile, right sheet on desktop). Fetches /legal/*.md on open with a module-level cache; renders via react-markdown (v10 already in deps) with hand-styled components (no typography plugin): serif forest headings, max-w-prose serif body, hairline hr, mono code, semibold strong. Scroll region = max-h-[85vh] overflow-y-auto scroll-quiet overscroll-contain; header = t-title + "Last updated 2026-09-17" (mono, muted) + built-in 44px close; loading = mono "Fetching the document…" (role=status); error = neutral-charcoal message + SecondaryAction retry (render-time-adjustment pattern keeps react-hooks/set-state-in-effect clean — first attempt hit that lint rule and was restructured); footer = MIT-as-is line + quiet note pointing to the repository.
   409→- Edited src/components/screens/landing.tsx — footer region only: LegalLinks under the trust-copy line at the same 11.5px scale (hero untouched).
   410→- Edited src/components/cc/settings-sheet.tsx — "About" section (header styled like "People here", two 44px rows with chevron + hover wash calling showLegal("terms"|"privacy"), t-meta caption), placed after "People here"; the burn block keeps its mt-12 whitespace separation. One self-inflicted bug caught by tsc during the edit (my insert swallowed SettingsSheet's `export` keyword) — fixed immediately.
   411→- Mounted <LegalSheetHost /> in src/components/cc/app.tsx next to SealingOverlay/BurnOverlay (the established global-overlay mount point) — diff kept to exactly two lines (import + element).
   412→- Verified: eslint clean on all four touched files; `bunx tsc --noEmit` clean for src/ and public/ (remaining errors are pre-existing and unrelated: examples/websocket/server.ts socket.io types, skills/image-edit, skills/stock-analysis-skill — noted, not fixed per instructions). Dev server not restarted; no new npm dependencies (react-markdown was already installed).
   413→
   414→Stage Summary:
   415→- Legal single-source-of-truth shipped: public/legal/{terms,privacy}.md are detailed (18 + 14 numbered sections), factually tied to the real architecture and the DESIGN.md threat model (including the honest residuals), MIT LICENSE at the repo root.
   416→- In-app reading surface shipped: legal-sheet.tsx (store + LegalLinks + LegalSheetHost), mounted once in app.tsx; entry points in the landing footer and the Settings "About" section; paper/forest styling, hairlines, scroll-quiet, keyboard-accessible Radix sheet, works at 360px.
   417→- Deviations, deliberate: (1) used the Sheet primitive (bottom/right) instead of a centered Dialog because the design system reserves centered modals for the irreversible burn moment — mobile bottom sheet is h-[92dvh], i.e. effectively full-screen; (2) the sheet-footer repository note is styled link-like but is plain text — no canonical repository URL exists anywhere in the repo (package.json has no repository field), and a fake URL was worse than a typographic reference; (3) LICENSE was created per instructions even though another task may own it — if a conflict arises, the texts are identical standard MIT.
   418→- Suggested follow-ups: link the docs from README.md (README owned by another task); if a canonical repo URL is ever decided, promote the footer note to a real anchor; operators self-hosting should be reminded (README/docs) to replace the "[Your Jurisdiction]" and contact placeholders in their own deployments.
   419→
   420→---
   421→Task ID: 22-d
   422→Agent: frontend-styling-expert (logo system) + orchestrator finish
   423→Task: Design "The Split Seal" brand mark and integrate it creatively across the UI; regenerate the icon set.
   424→
   425→Work Log:
   426→- Rewrote src/components/cc/mark.tsx: hand-tuned bezier wax-seal with a single -62° diagonal fracture; joint radii wobble (33.3/35.1 vs nominal 34) for a hand-pressed feel; split inner disc; ember glint in the fracture. Three variants: intact (seated offsets), cracked (halves jarred apart + ember flecks, animates once on mount), ring (single currentColor rim fragment for member inks/watermarks). Backwards-compatible props.
   427→- globals.css: seal-stamp (landing press-in), reseal-stamp (rotation flash in chat header), seal-half-a/b break-apart, seal-glint flare, seal-fleck ember drifts — all in the app's existing motion register, reduced-motion safe.
   428→- Integrations: landing hero stamps in then breathes; burn-overlay uses the cracked variant finale; chat-header re-stamps a small seal when the room key version bumps; bubble sender identity = 13px ring fragment in the sender's ink; room-list/chat empty states use low-opacity ring watermarks; sealing screen uses ring fragments as the argon2id "wax drip" dots; invite screen on the new mark.
   429→- scripts/gen-icons.ts (bun + sharp) regenerated public/icons/{icon-192,icon-512,icon-maskable-512,apple-touch-icon}.png and public/og.png (1200x630, seal + serif wordmark); public/icon.svg + logo.svg rewritten. Filenames kept stable so manifest/paths didn't move.
   430→- Orchestrator finished the agent's timeout remainder: layout.tsx openGraph + twitter card metadata (og.png, 1200x630 + alt), apple icon switched to the dedicated 180px apple-touch-icon.png; verified PNG dimensions via sharp; lint + tsc + vitest all green.
   431→- NOTE: the subagent hit a context deadline after completing the implementation but before verification/worklog; the orchestrator verified and closed it out.
   432→
   433→Stage Summary:
   434→- The generic concentric-circles seal is replaced everywhere by an authored mark whose fracture IS the product metaphor (E2EE seal + ephemerality crack + rotation re-stamp).
   435→- Identity system: every member is a shard of the same broken ring, drawn in their deterministic ink — brand and protocol now speak the same visual language.
   436→- Icon/PWA/OG assets regenerated from the same geometry (scripts/gen-icons.ts is rerunnable).
   437→
   438→---
   439→Task ID: 22-a / 22-e / 22-f
   440→Agent: orchestrator (Z.ai Code)
   441→Task: Task 20 "Exit the sandbox" — env/deploy/CI/SW layer (22-a), full two-origin QA (22-e), docs + handover (22-f). Reviewer's Task 20 = internal Task 22 (20/21 were taken by captions/reactions/silent-grace).
   442→
   443→Work Log:
   444→- 22-a CONFIG: src/lib/relay.ts now reads NEXT_PUBLIC_RELAY_URL (default same-origin "/relay/"); the hardcoded XTransformPort pattern moved to .env.local (sandbox-only, gitignored via .env* with new !.env.example exception). grep for XTransformPort/localhost:81 in src/ returns NOTHING (acceptance met). .env.example documents every variable (DATABASE_URL, RELAY_INTERNAL_TOKEN, NEXT_PUBLIC_RELAY_URL, NEXT_PUBLIC_SITE_URL).
   445→- 22-a DEPLOY: deploy/{Dockerfile.web (multi-stage bun→node:22-slim, standalone output, prisma generate, baked empty-DB template + entrypoint volume-seeding), Dockerfile.relay (bun, copies index.ts + src/lib/rate-limit.ts preserving relative imports), docker-compose.yml (web + relay + caddy:2, RELAY_PRESENCE_URL=http://relay:3004 for the cross-container /evict consult, db-data volume), Caddyfile ({$CADDY_SITE} auto-TLS, handle_path /relay/* strips the prefix so the relay's path:"/" sees /?EIO=4 — websocket upgrades pass), deploy/.env.example}.
   446→- 22-a CI: .github/workflows/ci.yml — bun install --frozen-lockfile → prisma generate → lint → tsc --noEmit → bun run test (92 property tests) → npm audit --audit-level=high (via npm install --package-lock-only; bun pm audit doesn't exist in this bun) → production build (DATABASE_URL=file:/tmp/ci.db, NEXT_PUBLIC_RELAY_URL=/relay/). tsconfig.json now excludes examples/ + skills/ (sandbox scaffolding was the only tsc noise; src/ is clean).
   447→- 22-a SW UPDATE FLOW: public/sw.js → VERSION cipherchat-v3, /legal/ added to asset patterns, activate posts {type:"cipherchat:updated"} to all clients. sw-register.tsx registers with updateViaCache:"none" and shows ONE persistent sonner toast ("A fresh seal is ready", Reload action) on controllerchange-after-first-controller or the activate message — never auto-reloads (drafts live in memory).
   448→- 22-a MISC: README "Running it" rewritten (two-terminal dev block fixed, production compose path, CI, performance note for argon2id on low-end Android, Legal section; deduped the doubled silent-grace table row; added departure-proof row). next.config.ts allowedDevOrigins = localhost, 127.0.0.1, *.space-z.ai (the sandbox preview proxy was getting /_next/* BLOCKED otherwise). layout.tsx metadataBase via NEXT_PUBLIC_SITE_URL + OG/Twitter cards pointing at /og.png. Removed relay-diag-tmp*.mjs junk from repo root.
   449→- 22-e QA (agent-browser, two origins localhost:81 + 127.0.0.1:81, SWs unregistered first): full golden path PASSED — create room H4X31BGEY8 (A) → B joins via link+password (argon2id) → bidirectional messages (sender ring-fragment avatars confirmed in DOM) → **B leaves: POST /leave 200 with the NEW ECDSA departure proof, A shows "Faint Ibis left. The room re-sealed for those who remain."** → B rejoins after rotation (ECDH-wrapped key under entry key) → view-once file with caption A→B ("Opened — the contents are gone" spent state) → burn (both origins show "Burned — this room is gone"). Live relay log (/tmp/relay-supervised.log — note: mini-services/relay-service/relay.log is a STALE artifact of an older instance, pid 319 writes to /tmp) confirms join/leave/rejoin/burn lifecycle. Legal sheets verified: Terms (18 sections) + Privacy (15) fetch and render from /legal/*.md, close cleanly. Landing: seal stamps in then breathes, footer legal links present. Dark mode: html.dark, Nightfall tokens, seal re-inks to #4a6453. Mobile 390x780: footer pinned to bottom (footer.bottom == viewport height), no overflow. Zero console errors on both tabs; dev.log all-200s.
   450→- 22-f DOCS: COMPONENTS.md — mark.tsx section rewritten for the Split Seal variants, sw-register.tsx update-flow section, new legal-sheet.tsx section. This entry closes Task 22.
   451→- Verification summary: bun run lint clean; bunx tsc --noEmit clean (src/); bunx vitest run 92/92 across 12 files.
   452→
   453→Stage Summary:
   454→- The app is no longer sandbox-bound: one plain VPS + `docker compose up` in deploy/ yields a TLS site (Caddy auto-HTTPS, /relay/ prefix-strip proxying to the blind relay); all URLs and secrets are env-driven (.env.example is the contract); CI gates lint/types/92 property tests/audit/build.
   455→- Task 19 acceptance residuals all closed: leave proof-of-possession (property-tested, idempotent replay), room-info/verifier/members per-IP rate limits, member cap as freshness-windowed soft cap (policy centralized in lib/admission.ts + tested), HKDF scalar range explicit ([1, n-1] bigint check, scalarToRoomIdentity), coordinator memberId confirmed server-issued cuid (not client-choosable — documented), replay watermark added to the DESIGN.md localStorage inventory, argon2d low-end expectation documented in README.
   456→- Legal shipped: MIT LICENSE, public/legal/{terms,privacy}.md (18+14 sections, factually tied to the real architecture and threat model), in-app sheets from landing footer + Settings → About.
   457→- Brand shipped: the Split Seal (intact/cracked/ring variants) integrated into hero stamp-in, burn finale, rotation re-stamp flash in the chat header, member identity ring fragments, empty-state watermarks, sealing wax-drip dots, regenerated PWA/apple/OG assets via scripts/gen-icons.ts.
   458→- NOT done here (documented residuals): (1) Docker is unavailable in this sandbox, so the compose-build two-origin E2E could not be executed HERE — it must run once on any Docker host (README documents the exact golden path); dev-server two-origin E2E passed fully. (2) VLM feedback on the 512px icon: reads slightly "mechanical gasket" rather than waxy — acceptable for the stationery voice, but a future refinement could vary rim stroke thickness slightly / round the break ends. (3) `next.config.ts` still has typescript.ignoreBuildErrors:true — CI's explicit tsc step is the real gate; flipping the flag can be considered once CI runs for real. (4) relay.log staleness: the supervised relay writes to /tmp/relay-supervised.log; harmless, but a future round could repoint it.
   459→- Priority recommendations for the next round: (a) run the compose-build E2E on a Docker host and fix whatever it surfaces (prisma engine tracing and sharp are the usual suspects); (b) push the repo to GitHub and watch the first real CI run; (c) optional polish: SW update-toast visual QA in a production build; (d) feature candidates from the backlog: presence-driven rotation grace tuning, relay join-member tokens, message search-within-room (local, memory-only), room member rename sync.
   460→
   461→---
   462→Task ID: 23
   463→Agent: lead (Z.ai Code)
   464→Task: Brand-new logo (The Vanishing Ink), creator-chosen room TTL (presets + custom + until-burned), and message TTL with seconds + custom lifetimes
   465→
   466→Work Log:
   467→- Logo — replaced "The Split Seal" with "The Vanishing Ink": a hand-fallen ink drop whose top frays into three rising flecks, the last one ember-warm. New mark.tsx exports InkMark (SealMark aliased) with variants intact/scattered/fleck/ghost (was intact/cracked/ring); all ~15 call sites updated (burn overlay → scattered, typing dots + sender shards → fleck, watermarks → ghost). CSS: seal-break/glint/fleck keyframes replaced by ink-lift-off (drop outline rests at 42% as a ghost of what burned), ember-flare, fleck-flee, and ink-rise (breathing flecks loop under .mark-breathe, reduced-motion guarded so flecks hold still). VLM-reviewed twice (first critique: flecks too small/clustered → enlarged to r 6.2/4.7/3.9, zigzag scatter, drop redrawn smaller, SEAT retuned) until "distinctive & non-generic, proceed with confidence".
   468→- Brand assets — public/logo.svg + icon.svg rewritten; scripts/gen-icons.ts updated to the new geometry; PNGs regenerated via sharp (icon-192/512, maskable-512, apple-touch-180, og 1200×630 with wordmark + italic tagline). VLM-verified centering after one fix pass (drop mass at 46%/52% of tile).
   469→- Message TTL — TtlChoice is now number (0=off); TTL_STEPS presets gained 15s/30s/1m; new TTL_MIN_SEC=5 / TTL_MAX_SEC=86400, isCustomTtl, clampTtl. format.ts: fmtTtlRemaining handles <60s ("42s") and days ("3d 04h"); new fmtTtlShort/fmtTtlLong. New shared TtlPicker (presets grid + custom editor with sec/min/hr units, live label, cap copy); composer's blind cycle button replaced by popover (desktop, via PopoverAnchor) / bottom sheet (mobile); settings default-lifetime grid now the same picker.
   470→- Room TTL — Prisma Room.expiresAt (nullable = until burned); POST /api/rooms accepts ttlSec (5min–30d validated, default 24h, lazy-sweeps expired rooms on create); GET room info returns expiresAt and 404s {expired:true}; members/verifier/leave treat expired as gone; new PATCH /api/rooms/:id (creatorToken-gated, 410 on already-expired) lets the creator adjust later. Client: RoomCard.expiresAt/closed, RoomSession.expiresAt, store createRoom/joinRoom capture it, adjustRoomTtl + closeExpiredRoom actions, JoinResult "expired" reason with honest copy on join + unlock surfaces. UI: RoomTtlPicker in create sheet (24h/1h/7d/30d/Custom/Until-burned), invite sheet closing line, entry system line ("This room closes in X — then it's gone for everyone"), header countdown (terracotta under a minute), room-card "closes X" line, creator adjust in settings, in-chat expiry watcher (toast + navigate + "Closed — its time ran out" ash-style card, swept next load).
   471→- Infra — db:push ran; dev-server daemonization fixed for this environment (double-fork supervisor at /tmp/cc-keepalive.sh; direct setsid/nohup children are reaped at tool-command teardown — the relay supervisor from a previous session survives the same way).
   472→
   473→Stage Summary:
   474→- All three user asks shipped and E2E-verified via agent-browser on the gateway origin (localhost:81): create with custom TTL (3min clamped to 5min minimum ✓), invite/system-line/header/card countdowns ticking ✓, 15s preset message burned in 15s ✓, custom 30s message burned ✓, room expired live → toast + navigation + closed card ✓, creator PATCH 1h → header "closes 59:58" ✓, until-burned room shows no clock ✓, burn overlay ghost mark VLM-verified ✓, expired room: GET 404 {expired}, join 404, PATCH 410, swept on next create ✓, mobile sheet pickers render ✓, expired-join copy on the invite screen ✓.
   475→- tsc clean, eslint clean, 92/92 tests pass, dev.log clean, console free of runtime errors (one transient Radix dev warning not reproducible).
   476→- Logo system is creatively integrated: typing dots are flecks, sender identities are flecks of the same vanishing ink in member colours, watermarks are drop ghosts, the seal-stamp entrance became the drop landing, burn leaves the drop's outline resting where the ink was.
   477→
   478→---
   479→Task ID: 24
   480→Agent: lead (Z.ai Code)
   481→Task: "Add cool animations to the UI and even the logo" — a full motion pass over The Vanishing Ink brand and every surface, with reduced-motion discipline held throughout.
   482→
   483→Work Log:
   484→- ENVIRONMENT REPAIR (blocking QA, found at round start): the sandbox was reset at 09:11 and `.env.local` — the sandbox-only relay URL — was never recreated, so every client fell back to same-origin `/relay/`, which the sandbox gateway does NOT forward: rooms could be created (REST) but no socket ever connected (composer stuck on "Reconnecting"). Fixed: `.env.local` recreated (NEXT_PUBLIC_RELAY_URL=/?XTransformPort=3003 + a fresh RELAY_INTERNAL_TOKEN); relay dev script now loads BOTH .env and .env.local (`bun --hot --env-file=../../.env --env-file=../../.env.local index.ts`) so the token reaches the relay and a future reset self-heals; relay restarted (handshake 200, presence endpoint 403/200 guard verified with/without the x-internal-token header). Also learned: Turbopack served a STALE compiled CSS after this session's edits (touch was not enough — an actual content change forces the recompile; if computed styles ever miss new rules, append a comment and reload).
   485→- LOGO — the landing hero became a choreography (globals.css + landing.tsx + mark.tsx): the drop now FALLS (drop-land, 580ms: gravity easing in, squash at impact ~220ms, rebound, settle), the three flecks SPLASH upward on impact (fleck-splash, 460ms + staggered delays), a soft ink HALO bleeds outward beneath the mark (halo-bleed, radial forest stain, 800ms), then the existing evaporation loop takes over. The trick that makes it possible: fleck-splash drives the INDEPENDENT translate/scale CSS properties while ink-rise drives transform — two animations, disjoint properties, one element; per-fleck staggers moved from inline styles to numbered classes (.ink-fleck-1/2/3) because one inline animation-delay would bind every animation in a multi-animation shorthand.
   486→- LOGO — at rest, hovering the hero mark lifts the flecks (-2.5px) and flares the ember (scale 1.4) via transitions on the same independent properties (450ms), so the loop never pauses.
   487→- LOGO — ghost watermarks (empty desk, creator/joiner empty rooms) now DRAW THEMSELVES: ghost variant gained a `draw` prop (pathLength=1 normalized stroke-dash draw-on, 1.3s, flecks surfacing at 520/780ms), then drift almost imperceptibly forever (ghost-drift, 13s ±6px, 1.6s delayed).
   488→- UI — screen transitions: every screen root (landing, invite, desk layout, chat ActiveRoom + LockedRoomView, desktop empty pane) takes its seat with screen-in (280ms fade + 10px rise); ChatScreen is now keyed by roomId so switching letters is switching pages (fresh scroll + entrance). The boot mark and the desktop empty-pane mark breathe.
   489→- UI — messages arrive from the side they were written on: msg-in-self (from the right margin) / msg-in-other (from the left), 260ms, one soft overshoot — replacing the uniform rise.
   490→- UI — the typing whisper: the three static dots are now three small flecks of the writer's ink rising and fading on a loop (whisper-fleck, 1.15s, staggered 200/400ms) — the mark's vanishing language, small enough to stay a whisper.
   491→- UI — a TTL countdown in its final ten seconds breathes (ttl-final, 1s opacity pulse) on top of its terracotta colour.
   492→- UI — dispatch flourish: two small flecks (one forest, one ember) rise from the send button as the letter leaves (send-burst pseudo-elements, 520ms, cleared by the pseudo's animationend landing on the button).
   493→- UI — Nightfall arrives as a turn: the theme icon pair is keyed by resolved theme after hydration (useSyncExternalStore guard — no effect-setState, lint-clean) and the new icon swings in from -80° (theme-turn, 420ms).
   494→- REGRESSION FOUND & FIXED (Task 23 fallout, caught by this round's E2E): every TTL room carries its entry system line ("This room closes in…"), so `messages.length === 0` was never true and BOTH empty states — the creator's "Invite someone to begin" (+ copy-invite CTA) and the joiner's "You won't see messages from before you joined" — plus their watermarks were dead in every TTL room (the default). Empty now means "no letters": system lines render above the empty state, which takes its seat around them.
   495→- A11Y polish: the mobile TTL bottom sheet gained an sr-only SheetTitle + aria-describedby opt-out (kills the Radix DialogTitle error and Description warning — console now fully clean through the whole flow).
   496→- next.config.ts: devIndicators:false — the Next dev-tools badge was overlapping the composer on small screens (VLM flagged it in every screenshot) and never exists in production.
   497→- Reduced-motion discipline: every new animation degrades under prefers-reduced-motion (the landing splash disabled by class, whisper flecks static at 55%, drift/ttl-final disabled, halo loses its delay, everything else 150ms single fades via the existing global rule).
   498→- Docs: DESIGN.md motion table rewritten for the full vocabulary (24 rows); COMPONENTS.md mark.tsx section was still describing the OLD Split Seal — rewritten for The Vanishing Ink with the draw prop and the hero choreography; theme-toggle section updated.
   499→
   500→Stage Summary:
   501→- E2E verified through the gateway (localhost:81) with a two-origin two-member run (B joined from 127.0.0.1:81): hero fall/splash/halo/hover all confirmed via computed styles AND VLM review ("Flawless execution"); msg-in-self + msg-in-other both directions; send-burst caught mid-flight; typing flecks ("Quiet Hare is writing…" with 3 whisper-flecks on the other origin); 15s TTL message counted down, pulsed under 10s, burned and vanished; empty states + watermark draw verified on creator, joiner, and after refresh+unlock; unlock sheet + mobile TTL sheet animate; theme turn caught mid-swing in dark mode; mobile 390px: footer pinned to the exact viewport bottom (780), no horizontal overflow; console and page errors fully clean.
   502→- lint clean, tsc clean, 92/92 property tests pass, dev.log clean.
   503→- Verification screenshots kept at /tmp/qa-24-*.png (landing mid-fall + settled, empty room, mobile landing/chat/TTL sheet, dark desktop, final desktop).
   504→- Known residuals for the next round: (1) both old cron jobs (391228, 391768) are dead ("exec limits exceeded") — a fresh one is armed (392841, fixed_rate 900s); (2) relay now writes its log to /tmp/relay-qa.log (previous /tmp supervisors were wiped by the sandbox reset — if the sandbox resets again, start it with `cd mini-services/relay-service && bun run dev` in the background); (3) Task 20 residuals unchanged: compose-build E2E still needs a Docker host, first real CI run still pending, next.config.ts typescript.ignoreBuildErrors still true; (4) candidate next features: local message search-within-room, member rename sync, presence-driven rotation grace tuning, SW update-toast visual QA in a production build.
   505→---
   506→Task ID: 25-b
   507→Agent: general-purpose subagent
   508→Task: iOS splash screens + Capacitor scaffold for CipherChat mobile app
   509→
   510→Work Log:
   511→- Read the room first: worklog Task 22–24 (deploy/CI/SW layer, Vanishing Ink rebrand, motion pass), gen-icons.ts (the og.png composition recipe), logo.svg/icon.svg, layout.tsx, DESIGN.md, README.md — then kept every composition decision inside the established pattern: flat paper, the verbatim mark geometry, the Georgia wordmark in the og charcoal #33342E.
   512→- scripts/gen-splash.ts (new): loads public/logo.svg at runtime and extracts its inner <g> by regex (the mark is never re-drawn here — single source of truth), verifies sharp/librsvg resolves the file's var(--forest, #3A4F41) fallback fills correctly (probe rendered 58,79,65 — the hex, not black), then MEASURES the mark's true ink bounds by rendering it once at 10× and scanning the alpha channel (x 26.1–71.9, y 10.5–84.4 in the 96 viewBox — taller than wide, the flecks climb). Each splash seats those measured bounds, not the square viewBox, so the visual centre lands exactly at (50%, 42%) of the screen; wordmark beneath at baseline = measured mark bottom + 0.85 × font-size, font 9.5% of css width (≈36.5% of the 26%-of-width mark), letter-spacing −0.016em (og's −1 at 64px), weight 600.
   513→- Generated 13 launch PNGs into public/splash/ via `bun scripts/gen-splash.ts`: 640x1136, 750x1334, 1125x2436, 1170x2532, 1179x2556, 1242x2208, 828x1792, 1242x2688, 1284x2778, 1290x2796, 1320x2868, 1536x2048, 2048x2732 — the full iPhone SE→16 Pro matrix plus the two iPad resolutions, 414x896 in BOTH @2 (XR/11) and @3 (XS Max/11 Pro Max). sharp png compressionLevel 9 + palette (q90); script self-checks every file's pixel dimensions and the total budget.
   514→- Programmatic visual verification of the representative 1170x2532 (390x844@3): all four corners + edge midpoints exactly rgb(244,241,235) paper; 48,926 forest-ink pixels and 1,527 ember pixels in the mark region; the drop bulb centre samples exactly rgb(58,79,65) = #3A4F41; 16,430 charcoal pixels in the wordmark band (Georgia text rendered, not fallback-blank); ember fleck samples rgb(233,169,125) ≈ #E8A87C. Total payload: 107,526 bytes (105 KB) across all 13 files — 8.5× under the 900 KB budget.
   515→- src/components/cc/apple-splash.tsx (new): server component exporting <AppleSplashLinks /> — one <link rel="apple-touch-startup-image"> per device with the standard Apple media-query formula (device-width: {cssW}px) and (device-height: {cssH}px) and (-webkit-device-pixel-ratio: {dpr}); no orientation qualifier (iOS reports portrait dimensions regardless of rotation — documented in-file); device table mirrors DEVICES in the generator with a keep-in-sync note. TypeScript strict, no client directive.
   516→- DEVIATION (deliberate, spec-compatible): layout.tsx is under concurrent work (forbidden list), so <AppleSplashLinks /> is wired into src/app/page.tsx — the thin server wrapper, NOT on the forbidden list — where React 19 hoists the links into <head> identically. Verified live: `curl localhost:3000/` returns all 13 links in the served HTML, and GET /splash/splash-1170x2532.png → 200 (8107 bytes, image/png). If the layout agent prefers to host them there, the component moves as one line.
   517→- Capacitor scaffold: `bun add @capacitor/core` + `bun add -d @capacitor/cli` (both 8.5.2, inert until `npx cap add`); capacitor.config.ts — appId "app.cipherchat.mobile", appName "CipherChat", webDir "public" (documented placeholder: remote-URL mode syncs it only for stray local assets), server.url from CIPHERCHAT_ORIGIN (default cipherchat.example.com, comment states the deployed origin must be exported before cap sync), cleartext:false + android.allowMixedContent:false by policy.
   518→- MOBILE.md (new, README's voice): (a) PWA install — Android Chrome ⋮→Install app, iOS Safari Share→Add to Home Screen, what works (standalone window + branded launch image, offline shell that never caches conversations, local notifications while the process is alive, badge where allowed); (b) Capacitor pipeline — prerequisites, CIPHERCHAT_ORIGIN, cap add/sync/open, @capacitor/assets generation (1024 icon + 2732 splash sources re-runnable from the existing generators), Android keystore/.aab/Play outline, iOS Archive/App Store Connect/TestFlight outline; (c) honest limitations — no web push by architecture (the blind in-memory relay holds no room keys, so it cannot compose an encrypted push payload and a plaintext one would leak contents to the push provider), local notifications only while the app process is alive, remote-URL shell is a frame not a bundle, plus the 4-step future-work sketch (VAPID keypair, subscription-on-join with its retained-metadata cost, ECDH-derived per-recipient notification key, sw.js push handler) and the residual that even content-free pushes leak timing to Apple/Google.
   519→- Verification: bunx tsc --noEmit clean (covers scripts/, capacitor.config.ts, src/ via the **/*.ts include); eslint clean on all touched files; dev server untouched and healthy (GET / → 200, dev.log clean).
   520→
   521→Stage Summary:
   522→- The installed home-screen app now opens on the Vanishing Ink seated on paper instead of a white flash: 13 per-resolution iOS launch images (public/splash/splash-{W}x{H}.png, 105 KB total) + the link component that serves them, live-verified in the rendered HTML.
   523→- Files created: scripts/gen-splash.ts, public/splash/*.png (13), src/components/cc/apple-splash.tsx, capacitor.config.ts, MOBILE.md. Files modified: src/app/page.tsx (one import + one component line — the only non-forbidden entry into the tree), package.json + bun.lock (Capacitor deps only, no script changes). Untouched by instruction: layout.tsx, app.tsx, store/app.ts, lib/notifications.ts, store/notices.ts, notice-stack.tsx, app-settings-sheet.tsx, public/sw.js.
   524→- Decisions worth keeping: the mark geometry is read from public/logo.svg at generation time (never re-drawn), the composition positions are set by alpha-channel measurement rather than eyeballing, and the wordmark reuses og.png's exact ink/spacing ratios — the splash, OG card and app all speak the same geometry. page.tsx wiring is the one deviation, chosen because layout.tsx is concurrently owned; relocating the component later is a one-line change.
   525→- Not done (by design / for the next round): a 1024×1024 icon + 2732×2732 square splash source for @capacitor/assets are described in MOBILE.md as one-line re-runs of the generators, not generated now (Capacitor native projects don't exist yet); Android 12+ splash and store signing remain pipeline steps for whoever runs `npx cap add` on a machine with Android Studio/Xcode; web push remains future work with its privacy price spelled out in MOBILE.md §3.
   526→
   527→---
   528→Task ID: 25
   529→Agent: lead (Z.ai Code) + general-purpose subagent (25-b)
   530→Task: Fix the notification overlap at the top of the screen; implement proper in-app and native notifications; turn CipherChat into an Android/iOS application (installable PWA + Capacitor scaffold)
   531→
   532→Work Log:
   533→- Task 25-b (subagent, parallel): iOS splash screens (scripts/gen-splash.ts loads logo.svg's inner <g> verbatim, alpha-scans true ink bounds, seats at 42% height; 13 PNGs in public/splash/ = 105KB total; apple-splash.tsx renders the 13 apple-touch-startup-image links, wired in page.tsx where React 19 hoists them); capacitor.config.ts (appId app.cipherchat.mobile, server.url from CIPHERCHAT_ORIGIN) + MOBILE.md (PWA install steps both platforms, full cap add/sync/open pipeline, honest limits incl. why web push is architecturally absent + 4-step VAPID future-work sketch); @capacitor/core 8.5.2 + @capacitor/cli 8.5.2 installed (inert).
   534→- Notification engine (lib/notifications.ts): NotifyPreview pref "content"|"sender"|"none" (localStorage cc:notify:preview, DEFAULT "sender" — the brand keeps its mouth shut until asked; applies to BOTH channels); pure bannerLine/describeNotice (files never quoted, text truncated at 140, room-name fallback) and decideNoticeChannel (inRoom→none, hidden→native, else banner); notifyIncoming called from the store's receive path (text + file cases, only for !self && !inRoom); showNativeNotice prefers registration.showNotification (the only path on Android Chrome / installed iOS PWA) with a page-constructor fallback (desktop dev) whose onclick focuses + dispatches cc:open-room; syncBadge/clampBadgeCount for the launcher badge (guarded setAppBadge/clearAppBadge).
   535→- The overlap fix, two layers: (1) In-app banners are IN FLOW — app.tsx gained a Shell wrapper (h-dvh flex-col for desk/chat, min-h-dvh for landing/invite) with NoticeStack sticky-top above the screens; a banner opens a grid track (notice-open 0fr→1fr, 360ms; card settles from −12px) so the app steps DOWN — nothing is ever covered. (2) Sonner toasts seat at --toast-top = calc(env(safe-area-inset-top) + 64px + var(--cc-notice-h)) — offset/mobileOffset accept CSS-var strings (verified in sonner 2.0.7 dist), and NoticeStack's ResizeObserver publishes --cc-notice-h live so a toast can never land on the header OR on a banner.
   536→- NoticeStack (cc/notice-stack.tsx) + store/notices.ts: one banner per room (replace-in-place, clock reset), max 3, 6.5s auto-dismiss, timers in the store; two-step exit (dismiss marks leaving → notice-close → remove on animationend); card = ink-fleck avatar in the sender's colour + alias + room + preview line, focusable role=button (Enter/Space), X dismiss; empty stack claims zero space and no safe-area padding; aria-live polite.
   537→- Native notification round-trip: sw.js v4 gained notificationclick (focus existing client + postMessage cipherchat:navigate, else openWindow /#/r/:id — boot lands in the locked room's unlock sheet via syncHash); sw-register.tsx restructured so the navigate ROUTES (SW message + cc:open-room CustomEvent) attach in dev too, while registration/update-prompt stay production-only.
   538→- Global settings: cc/app-settings-sheet.tsx (AppSettingsSheet + AppSettingsButton bell) — Notifications section walks all four permission states (default→Allow button; granted→3-option preview radio group + test notification; denied→re-enable copy; unsupported→banners-still-work copy) and explains banners; Install section via new hooks/use-install-prompt.ts (beforeinstallprompt captured, our UI asks; iOS 3-tap Share walkthrough; standalone check via useSyncExternalStore — no effect-setState, lint-clean); About rows. Entry points: desk header (bell beside New room) + landing header (bell beside theme toggle).
   539→- PWA/manifest: display_override ["standalone","minimal-ui"], launch_handler focus-existing, categories, app shortcuts (New room → /#/new, Your rooms → /#/rooms). BadgeSync mounted in the Shell — sums unread across room cards into setAppBadge.
   540→- Tests: task-25-notifications.test.ts (16 cases: channel decision matrix, all preview prefs × text/file, truncation, room-name fallback, prefs' node-env failure mode, badge clamp). Docs: DESIGN.md motion table + 5 new rationale lines; COMPONENTS.md sections for sw-register routes, notice-stack, app-settings-sheet, apple-splash.
   541→
   542→Stage Summary:
   543→- E2E verified through the gateway with an isolated second browser session for the sender (a background tab has document.hidden=true → native path — the banner only shows for a VISIBLE app, by design): banner rose on the desk while visible (banner:1, --cc-notice-h 68px), header pushed 0→56/68 exactly to stackBottom (headerBelowStack true, cardInsideStack true — desktop AND 390px mobile), toast-top tracked the banner live (calc(0px+64px+53px)), auto-dismiss returned headerTop to 0 and notice-h to 0px, banner click navigated to the room and cleared unread, all 4 letters present. Toast overlap fix measured: copy-invite toast top=64 vs chat header 0–57, noOverlap=true. Settings sheet verified in all permission states incl. headless-denied; mobile sheet + room VLM-passed ("no glitches"; composer anchored, nothing covered). Reload after the shell restructure: clean boot to the desk. Consoles and page errors clean in both sessions; dev.log clean.
   544→- tsc clean, eslint clean (one set-state-in-effect fixed via useSyncExternalStore), 108/108 tests pass.
   545→- Honest limits: SW-based native notifications register only in production builds (dev-exempt by long-standing policy), so the OS-notification path was verified by code review + the permission/settings E2E, not by an actual OS notification in this headless environment; web push (VAPID) is documented as future work in MOBILE.md — local notifications fire only while the app process lives.
   546→- Known residuals: (1) previous cron jobs dead — fresh 15-min webDevReview cron armed this round; (2) compose-build E2E + first real CI run still pending (Task 20 residual); (3) next.config.ts typescript.ignoreBuildErrors still true; (4) candidate next features: notification tap deep-link QA on a real device, room-level notification mute, local message search, member rename sync.
   547→
   548→---
   549→Task ID: 26
   550→Agent: lead (Z.ai Code)
   551→Task: Security review triggered by the user pasting the live DOM of the create-sheet password input (`<input id="cc-new-pass" type="text" value="quill-grove-harbor-orchard">`) with the prompt "I'm sure you can find out the breach here" — find and fix the breach.
   552→
   553→Work Log:
   554→- BREACH IDENTIFIED (three layers, from the user's paste):
   555→  (1) CRITICAL — `generatePassphrase()` in lib/identity.ts drew the room password — the root secret that feeds argon2id — from `Math.random()`: V8's xorshift128+ stream, non-cryptographic, predictable, and shared with unrelated app code (rotation jitter in store/app.ts draws from the same global stream).
   556→  (2) CRITICAL — 18 bits of keyspace: 24-word list × 4 picks without replacement = 255,024 candidates, while kdf.ts itself documents the verifier bundle as "a public offline oracle for password guessing" (fetchable by anyone with the room id). The whole space grinds through argon2id in under 7 single-CPU hours; memory-hardness cannot rescue a keyspace that small.
   557→  (3) PLAINTEXT LIFECYCLE (what the paste literally showed): React syncs the controlled value into the DOM `value` attribute (readable in the Elements panel / by DOM-scanning extensions even while visually masked — inherent to HTML, but the secret should not OUTLIVE its need); the create sheet kept the password in component state after the room was created; `autocomplete="off"` does not stop Chrome offering to save an ephemeral room password to its on-disk manager (defeating "keys live only in memory"); the reveal (eye) toggle was `tabIndex={-1}` — keyboard users could not operate it.
   558→- FIX 1 — lib/identity.ts rewritten: PASS_WORDS expanded 24 → EXACTLY 256 words in the app's stationery/nature voice (verified by script: 256 count, 256 unique, no dupes, max length 9, all original 24 retained; first draft was 282, trimmed the 26 weakest — spelling traps like fuchsia/cyclamen, obscure like rosin/verdigris/zither, abstracts like horizon/zenith/voyage). generatePassphrase now draws from `crypto.getRandomValues` with rejection-guarded unbiased sampling (one byte = one word at list size 256, zero modulo bias; duplicates skipped; buffer refilled in place), default raised 4 → 5 words = 2^40 candidates. One tooling lesson: a MultiEdit whose second edit fails may still have applied its first edit — always Read after a failed multi-edit.
   559→- FIX 2 — Retention policy, with one reversal: initially made RoomSession.password optional and dropped it for v2 rooms at derivation — tsc then surfaced the invite sheet (cc/invite-sheet.tsx:160), a LEGITIMATE consumer that re-displays and re-copies the password (masked, Show toggle) so members can bring someone in at any time; its own copy says "The password lives in this room's memory only". Reverted to required `password: string` in all sessions with an honest comment: memory-only, retained for the room's open lifetime (invite sheet re-share + legacy v1 epoch re-derivation), never on disk, refresh empties it. The REAL breaches were the generator and the keyspace, not this documented retention.
   560→- FIX 3 — Input hygiene: create sheet clears password state when it closes (landing.tsx, previously lingered until next open); locked-room unlock clears on success (chat.tsx); invite screen clears after successful join (invite.tsx); room-list unlock sheet already cleared on close.
   561→- FIX 4 — PasswordField (cc/fields.tsx): `autocomplete="off"` → `"new-password"` (the documented suppressor of Chrome's save-password prompt for ephemeral room passwords); reveal toggle now keyboard-focusable (tabIndex={-1} removed) with `aria-pressed`; callers can still override via props spread.
   562→- DOCS — DESIGN.md §5 gained a "Room passwords" bullet (CSPRNG + 256-word list + 40 bits + why: public verifier oracle; retention policy stated honestly; the inherent limit that input.value is always readable from the user's own browser — masking defends shoulder-surfing, not the user's machine). COMPONENTS.md: CreateRoomSheet section + identity.ts and session.ts table rows updated.
   563→- VERIFICATION — lint clean; tsc clean; 108/108 vitest. E2E via agent-browser through the gateway, two tabs/origins: create sheet shows type="password" + autocomplete="new-password" + a seeded 5-word phrase (coral-gull-kettle-fossil-ripple); reveal toggle flips type text↔password with tabIndex 0 + aria-pressed; room KZWEYYRF6G created; invite sheet shows the password as 24 masked bullets, Show reveals the SAME passphrase, invite link carries no password; session B joined from 127.0.0.1:81 with the passphrase (argon2id round trip on the new format) — bidirectional messages both directions (Faint Cormorant ↔ Hollow Marten); refresh on A locked the room, unlock with passphrase worked, locked field masked; create sheet re-seeded a fresh phrase (poplar-trail-sketch-silk-elm) on reopen; consoles clean on both tabs; dev.log all 200/201. VLM review of both screenshots: password field masked with eye icon, layouts clean. QA note: the composer's send button is button[aria-label="Send message"] (type=button) — a `button[type=submit]` click is a silent no-op, and the a11y tree nests the composer's VALUE as StaticText under the textbox, which once masqueraded as a sent message; use the aria-label selector. Screenshots: /tmp/qa-26-create-sheet.png, /tmp/qa-26-room.png.
   564→
   565→Stage Summary:
   566→- The breach is closed at its root: room passwords are now born from the CSPRNG over a 256-word list (5 words, 40 bits — the public-verifier offline attack moves from an afternoon to a multi-year project), password inputs mask by default, never trigger the browser's disk password manager, clear the moment their job is done, and their keyboard reveal toggle is accessible. The memory-only session retention is deliberate and documented (invite sheet re-share + legacy epoch derivation).
   567→- Residuals / next-round candidates: (1) the effective security of a 5-word password still assumes argon2id cost — a future round could offer a "stronger passphrase" option (6–7 words) or numeric suffix for paranoid rooms; (2) web push (VAPID) remains documented future work in MOBILE.md with its privacy price; (3) Task 20 residuals unchanged: compose-build E2E needs a Docker host, first real CI run pending, next.config.ts typescript.ignoreBuildErrors still true; (4) fresh webDevReview cron armed (393241, fixed_rate 900s, tz Africa/Lagos) after the previous one (393168) died of exec limits.
   568→
   569→---
   570→Task ID: 27
   571→Agent: lead (Z.ai Code)
   572→Task: User challenge follow-up — "while typing using DevTools, in sensitive fields like passwords, the value=\"\" tag always reveals it. Can't that be hidden or removed?" Remove the DevTools value-attribute mirror from all password fields.
   573→
   574→Work Log:
   575→- DIAGNOSIS: React controlled inputs mirror every keystroke into the DOM `value` *attribute* (that is what the user saw live-updating in the Elements panel), AND the plaintext sat in React state (readable in React DevTools). The DOM `value` *property* is the irreducible minimum (the page must read the secret to encrypt; `$0.value` is a platform fact, true of every site) — but the attribute mirror and the state copy are both removable.
   576→- FIX 1 — fields.tsx PasswordField rewritten as the **uncontrolled secret field**: props now `Omit<InputHTMLAttributes, "value" | "defaultValue">` (a compile error to pass either). React therefore never writes the value attribute — Elements shows NO value attr for the field's whole life, and no plaintext ever sits in React state. Parents seed/wipe imperatively through the forwarded ref (`el.value = …` sets the property only) and read once at submit. Eye toggle / autocomplete="new-password" / masking unchanged.
   577→- FIX 2 — all four consumers migrated to the ref pattern: landing CreateRoomSheet (seed on open + "New password" reseed + read-at-submit + wipe-on-success), invite InviteForm, chat LockedRoomView, room-list UnlockSheet (wipe in close()).
   578→- SUBTLE BUG FOUND & FIXED during QA: the create-sheet seed effect initially lived on the parent (`useEffect([open])`) — probe showed it fired with `passRef.current === null`. Radix Presence mounts portal children in a LATER commit than the parent's open-state change (mount transition dispatched from a useLayoutEffect). Fix: `SeedPassphrase` — a render-null child INSIDE the portal content whose mount effect seeds the field exactly when the input comes to life (and re-runs on every reopen since closing unmounts the content).
   579→- UNRELATED REAL BUG FOUND & FIXED — the "offline / The line is down" state visible in the user's pasted screenshot: `.env` was missing `NEXT_PUBLIC_RELAY_URL`, so relay.ts defaulted to `/relay/` — a path prefix that only exists in the docker-compose deploy (deploy/Caddyfile handle_path). The sandbox gateway (:81) routes by `XTransformPort` query instead, so the browser's socket.io hit Next.js and 404'd → permanent offline, composer disabled. Fix: `.env` now sets `NEXT_PUBLIC_RELAY_URL=/?XTransformPort=3003` (the .env.example documents exactly this dev-sandbox case) + dev server restarted.
   580→- DOCS: DESIGN.md §5 "Room passwords" bullet extended with the uncontrolled-secret-field pattern and the honest attribute-vs-property distinction; COMPONENTS.md fields.tsx section rewritten (was stale: still claimed tabIndex=-1 and autocomplete="off") + CreateRoomSheet section updated.
   581→
   582→Stage Summary:
   583→- VERIFIED (agent-browser, both localhost:3000 and through the gateway 127.0.0.1:81): create sheet seeds a fresh 5-word CSPRNG phrase with getAttribute('value') === null at every step — seeded, eye-toggled (type flips text↔password, property preserved), typed-into, reseeded via "New password" — the attribute NEVER appears; submit reads the ref, room E4PMRV5MES created (201/200s in dev.log), field wiped. Hard reload → locked view field also attr:null → typed full 31-char password (attr stayed null) → Enter unlocked (argon2id round-trip on the uncontrolled read). Invite link join (second origin, gateway): field attr:null, typed, Enter → joined + wiped. Room-list unlock sheet: attr:null. Live message sent and delivered through the relay once the env fix landed (composer enabled, no line-down). Consoles clean, no page errors, dev.log clean, VLM review of the room screenshot: "clean and functional". lint clean, tsc clean, 108/108 vitest.
   584→- The DevTools answer, stated for the user: the *attribute* is now gone (nothing to read in Elements); the *property* (`$0.value`) remains by design — that is true of every password box on every website and is the user's own machine.
   585→- Residuals: (1) previous crons may be dead — re-armed this round if missing; (2) Task 20 residuals unchanged (compose-build E2E needs Docker, first CI run pending, next.config.ts typescript.ignoreBuildErrors still true); (3) invite-sheet still renders the stored session password as a text <span> on explicit "Show" — deliberate (re-sharing), masked by default; (4) future hardening candidate: offer 6–7-word "paranoid room" passphrases.
   586→
   587→---
   588→Task ID: 28
   589→Agent: lead (Z.ai Code)
   590→Task: User request — "write all the changes and additions that were made in a doc. Be detailed and specific as possible. When done, suggest improvements/additions."
   591→
   592→Work Log:
   593→- Read the full worklog.md (Tasks 0–27) end-to-end in chunks, plus DESIGN.md/COMPONENTS.md/MOBILE.md spot-checks, to compile the complete history.
   594→- Wrote /home/z/my-project/CHANGES.md — a detailed chronological change record in 12 sections: (1) foundation Tasks 0–8 (architecture, design system, backend, client libs, all 9 screens, the 9 first-round bug fixes, polish pass, DESIGN.md); (2) feature rounds 1–3 (typing indicators, attachments×3, time dividers, char limit, grabbers, invite restructure, scroll pill, copy menu, PWA SW, the mobile keyboard fix with --kb-inset); (3) protocol v2 hardening (replay, uniform padding, argon2id, per-room identity, real rotation, adversarial review fixes); (4) round 20 (file captions fix, ink reactions, escape-to-rooms, SW dev guard); (5) round 21 (silent-departure grace, drafts, auto-focus, offline strip, unread counts, the two E2E-caught bugs); (6) Task 22 (leave proof-of-possession, rate limits, admission soft cap, legal package, deploy/CI/env layer); (7) Task 23 (Vanishing Ink mark, room TTL, message TTL); (8) Task 24 (motion pass + empty-state regression fix); (9) Task 25 (notification engine, in-flow banner overlap fix, notice stack, SW round-trip, settings sheet, iOS splash, Capacitor); (10) Task 26 (password breach: CSPRNG 256-word list, 2^40 keyspace, input hygiene); (11) Task 27 (uncontrolled secret fields, SeedPassphrase, relay env fix); (12) current state inventory (tests, services, docs, deploy, honest residuals).
   595→- Every entry is specific: file paths, constants, algorithms, parameter values, bug root causes, and the operational lessons that future rounds depend on.
   596→
   597→Stage Summary:
   598→- CHANGES.md is now the fourth documentation pillar (README = product, DESIGN = rationale, COMPONENTS = reference, CHANGES = history, worklog = raw journal). Improvements/additions suggestions were delivered in the chat reply (not baked into the doc) per the user's phrasing.
   599→- No code changed this round — documentation only. lint/tsc/tests untouched and still green from Task 27.
   600→
   601→---
   602→Task ID: 29-g
   603→Agent: docs-agent (Z.ai Code)
   604→Task: Documentation for round 29 (quoted replies + in-app media viewers)
   605→
   606→Work Log:
   607→- Read the room first: worklog.md Tasks 22–28 (deploy/CI layer, Vanishing Ink, motion pass, notifications, the two password-hardening rounds, CHANGES.md's birth) plus all three target docs end-to-end, to match the quiet rationale-first voice and the layer-cake structure before writing a line.
   608→- Verified every claim against the shipped code, not just the brief: `src/lib/types.ts` (ReplySnapshot, REPLY_SNIPPET_MAX=120, makeReplySnapshot — "Sealed message" for view-once, file name + `file:true`, whitespace-collapsed 120-char text; sanitizeReplySnapshot caps 64/64/160; isReplySnapshot; MessageView.replyTo), `protocol.ts` (replyCanonical unit-separator slots, canonicalV2 12th field, sealFrame extraction), `room-protocol.ts` (sealText/sealFile reply, AssemblingFile map, completed-file OpenResult, open() verifies the RAW snapshot before sanitising), `store/app.ts` (sendMessage 4th param, optimistic replyTo, both receive paths sanitise), `bubble.tsx` (QuoteBlock with border-l-2 in the quoted sender's ink, data-mid rows, three reply affordances — context menu / hover icon at -left-10 self / -right-10 others / double-click, FileContent Eye-Film-Music open-the-viewer cards), `composer.tsx` (reply slip, focus-on-begin incl. touch, Escape stopPropagation), `chat.tsx` (replyTarget + viewing released by render-time adjustment when their message burns, jumpToMessage with CSS.escape + flash restart, beginReply closes the viewer, window-level Escape guard steps aside while replying), `file-viewer.tsx` (classifyFile + TEXT_EXTS, blob lifecycle keyed on message id with revoke cleanup, TEXT_SHOW_MAX=100_000, CSV_MAX_ROWS/COLS 500/32, parseCsv, HexDump first 512 bytes, PdfView canvas + devicePixelRatio cap 2 + bytes.slice() copy + iframe fallback / view-once stays sealed), `globals.css` (quote-flash 1300ms + static 8% reduced-motion tint), `eslint.config.mjs` (public/** ignore), `package.json` (pdfjs-dist ^6.3.289), and the 16-case test file's full describe/it inventory.
   609→- DESIGN.md — §2 inventory rows updated (MessageBubble + quoted reply state; FileCard now opens the viewer; Composer + replying state; FileViewer row rewritten); §3 motion table gained the quote-flash row (1300ms once, reduced-motion static 8% forest tint); §4 gained four rationale lines (a quote is a copy not a reference; view-once targets are quoted as "Sealed message"; files open in the app, never into the browser's hands; a view-once file has no download anywhere); §5 gained two architecture entries — **Quoted replies** (snapshot design, the canonical 12th field / signature coverage, raw-before-sanitize verification, size-indistinguishability, and the honest mixed-version failure mode: an old tab drops new reply frames rather than accept an unverified quote — lost for that tab, never forged) and **In-app viewing** (revocable blob-URL memory-only policy, view-once no-download stance, pdf.js-on-canvas choice with the worker vendored at public/pdf.worker.min.mjs, HTML-never-executed, SVG-only-via-img, and sealed-over-fallback for view-once PDFs).
   610→- COMPONENTS.md — ChatScreen section gained the "Round 29 state" block (replyTarget/viewing burn release, jumpToMessage mechanics, beginReply); Composer table gained the Reply slip row; bubble.tsx section gained the QuoteBlock paragraph, the reply affordances in MessageBubble, "Reply" as the first CopyMenu item, the rewritten FileContent list (opening is every card's job now), and the downloadFile caller note; the FileViewer section was completely rewritten (classification, blob lifecycle, per-type viewers including the full PDF canvas story, header/buttons, view-once policy with the by-design note); the chat keyboard layer paragraph now states the reply-first Escape priority; §1's data-flow note covers replyTo snapshots as data-not-secrets; §11 gained the sendMessage signature, the receive-path sanitize note, updated lib rows for types.ts / protocol.ts / room-protocol.ts, and the __tests__ row now lists the whole suite (124 cases).
   611→- CHANGES.md — header test count 108→124; new "## 12. Round 29 — quoted replies + in-app media viewers" section in the established style (12.1 replies with file paths, constants and decisions; 12.2 the viewers with every per-type behaviour and the eslint ignore; 12.3 the 16 test cases + the full E2E QA story — room CMJ1NT8144 through the gateway, corrupt-PDF iframe fallback, view-once no-download + spent propagation, VLM review, 124/124); "Current state inventory" renumbered to §13 and refreshed to 124/124 across 14 files.
   612→
   613→Stage Summary:
   614→- All three documentation pillars now describe round 29, with every technical claim cross-checked against the shipped code (pdfjs-dist v6.3.289; quote-flash 1300ms with the static 8% reduced-motion tint; sanitiser caps 64/64/160; CSV caps 500×32; TEXT_SHOW_MAX 100_000 — all confirmed in source, not carried over on trust).
   615→- DESIGN.md carries the two security rationales where protocol decisions live (§5): the signature-covered quote — canonicalV2's 12th field, raw-before-sanitize, mixed-version sessions failing closed — and the memory-only in-app viewing policy — revocable blob URLs, no download for view-once anywhere, canvas-over-browser-toolbar for PDFs, sealed-over-fallback, HTML never executed, SVG only through an <img>.
   616→- CHANGES.md's history is complete through round 29 with the QA story recorded as verified, and the closing inventory reflects the current state (124/124, 14 test files).
   617→- No code, config, or tests were touched — documentation only; nothing to re-verify beyond the edits themselves.
   618→
   619→---
   620→Task ID: 29
   621→Agent: lead (Z.ai Code)
   622→Task: User request — "Add being able to reply to specific messages, add in app viewers for view once docs and picture and every other type of media instead of downloading it which defeats the whole point. Add all the improvements. As long as there's no security compromise."
   623→
   624→Work Log:
   625→- 29-a PROTOCOL: types.ts gained ReplySnapshot {id, senderId, snippet, file?} + REPLY_SNIPPET_MAX=120 + makeReplySnapshot() (view-once targets quote as "Sealed message" — contents never summarised; file targets quote the name with file:true; text collapses whitespace, first 120 chars) + sanitizeReplySnapshot() (post-verification UI gate: ids capped 64, snippet 160, malformed dropped) + isReplySnapshot() guard; MessageView.replyTo. protocol.ts: FrameBody.reply; NEW replyCanonical() (deterministic unit-separator serialisation); canonicalV2 appends the reply as its 12th component — THE QUOTE IS SIGNATURE-COVERED, so nobody can forge a quote of words their author never wrote (a tampered snippet breaks the ECDSA verification). sealFrame extracts+signs it. room-protocol.ts: sealText/sealFile accept reply; the file assembler carries it through chunk reassembly into the file OpenResult; open() verifies against the RAW reply (verification must recompute the sender's exact canonical, never a cleaned-up one).
   626→- 29-b STORE: sendMessage(text, file?, ttlOverride?, reply?); optimistic view carries replyTo; text + file receive paths sanitise for the view.
   627→- 29-c REPLY UI: bubble.tsx QuoteBlock (quoted sender's own ink colour on the left border, alias+snippet, click jumps to the original with scrollIntoView center + quote-flash 1300ms forest pulse; missing originals toast "no longer in this session"); three affordances — context-menu Reply (touch press-and-hold), hover-only icon button beside the bubble (desktop), double-click; data-mid on every row. composer.tsx reply slip above the input (quote + X, focus on begin incl. touch, snapshot sealed at submit); Escape while replying releases the quote FIRST (stopPropagation beats the window-level escape-to-rooms). chat.tsx: replyTarget state (cleared on room switch + when target burns, render-time adjustment), jumpToMessage with CSS.escape + flash restart.
   628→- 29-d VIEWERS (file-viewer.tsx REWRITTEN): classifyFile() routes image/video/audio/pdf/text/csv/binary by mime+extension; every file decodes once to memory and renders through a REVOCABLE blob: URL (effect keyed on message id, revoke on switch/unmount — plaintext never touches disk through us). Images zoom; video <video controls playsInline controlsList="nodownload noremoteplayback"> (PiP + contextmenu suppressed for view-once); audio card with the same stripped controls; PDFs RENDER VIA pdfjs-dist v6 ON A CANVAS (bun add pdfjs-dist; worker vendored at public/pdf.worker.min.mjs; dpr scaling, fit-width, Back/Next, "Page x of y" — canvas means NO browser PDF toolbar and NO save button); pdf.js load failure: regular files fall back to a blob iframe, VIEW-ONCE FILES STAY SEALED (an honest notice — the fallback would expose the toolbar's save); render failure (corrupt PDF) = failed state, not a blank canvas. Text renders as escaped source in a monospace pre (wrap toggle, 100KB display cap, "copy takes the whole file"; HTML labelled "shown as source — HTML is never executed here"); CSV parses through a quote-aware parseCsv() into a sticky-header table (500 rows × 32 cols caps); binaries get a metadata card + 512-byte hex dump. Header: name · size · mime + Copy + Download (Download EXISTS ONLY FOR NON-VIEW-ONCE) + Close; view-once carries the by-design note "There is no download for this file, from anyone." bubble.tsx: ALL file cards now open the viewer (eye glyph; Film/Music icons for video/audio) — downloading is a choice inside the viewer, never the card's whole job. Viewer closes when its message burns. eslint.config.mjs ignores public/** (the vendored worker).
   629→- 29-e TESTS: task-29-replies.test.ts — 16 cases: text + file-assembly round-trips, relabel rejection, size-indistinguishability from plain text, replay refusal, replyCanonical determinism, DIRECT ECDSA PROOF that changing the quote breaks the signature, snapshot building (truncation/file/sealed), sanitiser caps/drops, classifyFile routing, parseCsv (quotes/commas/newlines/CRLF/single-column). Suite 108 → 124, all green; tsc clean; eslint 0 errors.
   630→- QA WAR STORY (worth remembering): a two-line JSX comment containing an em dash made BOTH tsc and eslint fail with "Identifier expected" at a perfectly valid <div> — byte-level dumps (od/base64/codepoints) showed nothing wrong, minimal repros parsed fine, and tool I/O itself display-mangles "[m" sequences (pb-[max(...)] reads as pb-ax(...)). The fix was sed-replacing the comment by LINE NUMBER; the forensic lesson is in the pipeline, not the file.
   631→- 29-f E2E (agent-browser, two sessions through the gateway 127.0.0.1:81, room CMJ1NT8144 "QA 29 Gateway"): B→A message; A replied via the hover button — quote block rendered on BOTH sides with the right alias+snippet; quote click jumped + flashed; context-menu Reply worked; Escape cancelled the reply WITHOUT leaving the room; reply to a spent sealed message quoted "Sealed message". Viewers: text (pre + copy + download), CSV table (header + 3 rows), audio (blob src, controlsList verified), video (real 2s ffmpeg mp4 — played, readyState 4, PiP allowed as non-view-once), PDF canvas at desktop AND 390px mobile ("Page 1 of 1", ~563 painted text pixels; mobile canvas 326px wide, VLM-confirmed the fixture text visible); a deliberately corrupt PDF took the iframe fallback exactly as designed. VIEW-ONCE: sealed card both sides → A opened → content shown, NO download button, by-design note present → spent card "Opened — the contents are gone" propagated to both sessions. pdf.js teardown hardened (.catch on destroy) after unhandled-rejection forensics. In-page error listeners on a clean load: ZERO errors; dev.log all 200/201. Screenshots: /tmp/qa29-desktop-room.png, /tmp/qa29-desktop-video.png, /tmp/qa29-mobile-pdf-fixed.png (+mobile-room) — VLM-reviewed clean. NOTE for future QA: session A originally ran on localhost:3000 where the XTransformPort relay URL cannot route — BOTH sessions must use the gateway origin.
   632→- 29-g DOCS: delegated to the docs agent (see the Task ID 29-g section above) — DESIGN.md (§4/§5 rationale incl. the honest mixed-version failure mode: an old tab DROPS new reply frames rather than accept an unverified quote), COMPONENTS.md (file-viewer rewrite, QuoteBlock, reply wiring, lib tables), CHANGES.md (Round 29 section, suite count 124).
   633→
   634→Stage Summary:
   635→- Both requested features shipped with no security compromise — replies are signature-covered end-to-end (the strongest authenticity story in the app: a quote is exactly as unforgeable as the words it carries), and in-app viewing means the plaintext of a view-once file exists only as memory behind a revocable blob URL with no download path anywhere, by design. All media families view in-app: image zoom, video, audio, PDF-on-canvas (pdfjs-dist v6, worker vendored in public/), text/HTML-as-source, CSV tables, hex inspection for binaries. 124/124 tests, tsc clean, eslint 0 errors, two-session E2E green through the gateway including mobile width.
   636→- Residuals / next-round candidates: (1) the reply quote survives its original burning BY DESIGN (the words were already shown) — if that ever feels wrong, a "quotes burn with their original" policy is a one-line change in the burn path; (2) video thumbnails are icon cards, not frame previews (canvas frame-capture is possible but adds decode cost); (3) pdfjs-dist adds ~1.2MB vendored worker + lazy chunk — acceptable, documented; (4) image EXIF is NOT stripped on send (browsers strip it on canvas re-encode only) — a future round could re-encode images through a canvas before sealing; (5) Task 20 residuals unchanged (compose-build E2E needs Docker, first CI run pending, next.config.ts typescript.ignoreBuildErrors still true); (6) fresh webDevReview cron armed this round if missing.

[showing lines 337-636 of 637; use offset=637 with limit to continue reading]