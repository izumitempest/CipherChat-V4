{
  "name": "cipherchat-relay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --hot --env-file=../../.env --env-file=../../.env.local index.ts"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}